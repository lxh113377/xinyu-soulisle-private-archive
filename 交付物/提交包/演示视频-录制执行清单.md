# 心屿 SoulIsle · 演示视频「录制执行清单」（L3）

> 用途：把 `演示视频脚本.md`（4分30秒分镜表）变成**当天照着做就能录完**的操作单。
> 视频本体必须真人录制（AI 不能代出 MP4），本清单负责：**录前实测 → 分镜操作 → 录后验收**。
> 立此单原因：提交清单第 2 项「演示视频 MP4」此前只在台账外，是**漏登记的提交物**（2026-09-23 补登）。

## 〇、录前必跑（全绿才开录）

```powershell
# 1) 双线在线都活着（视频里要出现「在线」徽章）
(Invoke-WebRequest https://xinyu-soulisle.pages.dev -UseBasicParsing).StatusCode              # 200
(Invoke-WebRequest https://qwer-...tcloudbaseapp.com -UseBasicParsing).StatusCode            # 200

# 2) 国内线 AI 对话真能通（不是只开得了页面）
curl.exe -s -o NUL -w "%{http_code}" -X POST https://qwer-d4gf2r76o8829463b.service.tcloudbase.com/api `
  -H "Content-Type: application/json" --data-binary '{"messages":[{"role":"user","content":"hi"}],"max_tokens":8}'
# 期望 200（2026-09-23 实测：200 + 真实回复 + CORS 放行）

# 3) 情绪引擎指标（旁白/字幕若报数字，必须与此处一致）
node _test/emotion_eval.js            # 73 条 / 98.6% / 危机召回 6/6
# 4) 语音输入可用（1:50 分镜若用到）
python _test/voice_check.py http://127.0.0.1:8125/   # 期望 VOICE-PASS
# 5) 前端未回归
python _test/browser_check.py         # 期望 ALL-ASSERT-PASS
```

> ⚠️ **数字口径**：2026-09-23 起评测集由 **36 条 / 94.4%** 变为 **73 条 / 98.6%**。
> 视频字幕、PPT、方案 PDF 三处只要出现旧数字，必须同步改（这是评审挑刺的高发点）。

## 一、分镜 × 操作 × 验收

| 时间 | 分镜 | 具体操作 | 录完即验 |
|---|---|---|---|
| 0:00-0:20 | 片头 | 无痕窗口开 `https://xinyu-soulisle.pages.dev`，缓慢滚第一幕 | 星雾在呼吸（不是静止） |
| 0:20-1:00 | 痛点 | 切到数据页 | **"示例数据"角标常驻**（无角标 = 诚信红线） |
| 1:00-1:50 | **AI运行过程** | 输入「被导师批评了」，左屏开着情绪解析面板 | 左=先出词典得分、后出 LLM 精判；右=星雾变蓝。**两段都要拍到** |
| 1:50-2:40 | 多模块协同 | 连聊 3 轮 | 第四幕情绪曲线**同步生长**；徽章显示「在线大模型生成·xxx ms」 |
| 2:40-3:10 | 安全边界 | 输入含危机词的一句话 | 星雾红色脉冲 + 热线卡片弹出（**生成被拦截**） |
| 3:10-3:40 | 降级演示 | 断网 | 徽章变「离线共情模板」，对话仍能继续 |
| 3:40-4:10 | 隐私与记忆 | 点「清除我的数据」 | 曲线归零 |
| 4:10-4:30 | 片尾 | 露出链接 + 二维码 | 手机扫码能开 |

## 二、录后验收（机器判据，不靠肉眼）

```powershell
# 时长硬约束 ≤5min（300s）
ffprobe -v error -show_entries format=duration -of default=nw=1:nk=1 演示.mp4   # ≤ 300
# 分辨率
ffprobe -v error -select_streams v:0 -show_entries stream=width,height -of csv=s=x:p=0 演示.mp4  # 1920x1080
```

## 三、已知未做

- **视频本体 0 条**（全项目 `*.mp4` = 0），需真人录制。
- **二维码未生成**。
- 本机无 `ffprobe` 实测记录 —— 上面两条命令**未在本机跑过**，录完当天须真跑一次再交（判据先实跑再写进文档）。
