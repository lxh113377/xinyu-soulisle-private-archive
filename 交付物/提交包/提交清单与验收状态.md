# 提交清单与验收状态（心屿 SoulIsle）

> 截止：2026-09-30（报名+提交，截止后团队信息锁定）｜ 赛事口径：2026年iCAN大学生创新创业大赛·AI应用创新挑战赛·软件赛道·高校组

| # | 提交物 | 官方硬约束 | 当前状态 | 验收判据 |
|---|--------|-----------|---------|---------|
| 1 | 应用方案 PDF | ≤20页，9项内容齐全 | ✅ **2026-09-25 r32 第三次修订**：`交付物/提交包/心屿SoulIsle-应用方案.pdf` **20 页 / 9 图**（官方 9 项逐项齐全；**页数已贴上限，再加内容必须先精简**）。本轮按 R242 回扫"交付材料落后于构建"：方案正文此前对 `sw.js`/`Service Worker`/`SSE 逐字流式`/`设置面板` **0 命中** ⇒ 新增 **6.7 离线可用（浏览器应用壳）**、**7.8 评委自助：设置面板与模式自证** 两节 + 实拍 **图 8/图 9**，7.3 补 SSE 流式一句；6.6 耗时行由单次样本改为两次独立实测区间（965 ms / 1,378 ms）。历史：09-24 产出 + 二次修订（s01/s08 重截、修 Edge 页脚泄露本机路径）。复算：`powershell -File 交付物/提交包/render-pdf.ps1`（页数与图数从 HTML 现读，不手抄）+ `python _test/pdf_leak_scan.py` | 逐条对照9项勾选✅；无未验证承诺✅；**PDF 文本层泄露复扫 CLEAN**（`file://`/家目录/`sk-`/邮箱/手机号 五类零命中，且自测证过"噪声串不误报、真敏感抓得住"）✅ |
| 2 | 演示视频 MP4 | ≤5分钟，突出智能体运行过程 | ✅ **成片已交付并于 2026-09-25 两次重录（r30 首录 → r34 再录）**：`交付物/提交包/demo_video_out/心屿SoulIsle-演示视频.mp4`，8 幕 218.5s（旧行写「视频本体 0 条，需真人录」与磁盘实况不符，已按实测更正）。重录动因：旧片画面仍是**情绪后端化之前**的，而"后端引擎 + 同源代理在线"才是最有说服力的两点差异。S3 实测标签：`在线大模型生成 · 逐字流式 · 情绪双路：词典+LLM 一致 → LLM · 情绪:后端 · 1378ms`；**r34 再录动因**：r30 成片录于 16:54，早于 r32 两处诚实性修复（`app.js 18:27` 徽章三态 / `chat-window.js 18:35` 开场白标签）⇒ 画面里开场白仍挂着会被读成「模型说的」的旧标签。新片抽帧目检确认画面含 **`本机开场白 · 未经大模型`** 与右上角 **`● 在线 AI`**，S3 标签 `… · 情绪:后端 · 1834ms · 情绪：低落` | `ffprobe` 时长 **218.48s** ≤300s✅；`RECORD-PASS scenes: 8 end_t_ms: 217428` + `VIDEO-PIPELINE-PASS dur=218.5s size=23.0MB`（逐幕机器断言，在线判据不成立即 exit 1）；成片 **sha256=`04a7f7bf2e70b3646145935e5ac3a91bc9023ca32a9c10f397b074420888ec7e`**（22,954,501B）；代际可追：r29 `50e060d1…` → r30 `fc810f65…` → r34 `04a7f7bf…` 三者互不相同已核验；复算（**执行目录 = 仓库根绝对路径**，前置服务 = 定版 fat jar `java -jar server/target/soulisle-server.jar --server.port=8123`，`GET /api/health` 须见 `"stage":"J5"` + `indexFound=true` + `vendorFound=true`）：`python _test/demo_video_pipeline.py`（前置与配置切换见下方红线；r34 实测还原后 `src/js/demo-config.js` sha256=`2521954c4f20…` 与登记值**逐字节相等**，公网零密钥 stub `deploy/xinyu/js/demo-config.js` sha256=`00024e4dbaf3…` **未被触碰**，`python _test/public_check.py` → `KEY_LEAK: False` + `PUBLIC-ONLINE-ALL-PASS`） |
| 3 | 可运行程序 | 链接稳定可访问 或 源码 | ✅ 双线：①主推 **https://xinyu-soulisle.pages.dev**（**2026-09-25 r33 重部署**，deployment `b5f46ecf`：`LIVE-SYNC-PASS` 逐字节等 + `PUBLIC-ONLINE-ALL-PASS`（标签实测「在线大模型生成 · 逐字流式」/ 危机 True / CONSOLE_ERRORS 0 / KEY_LEAK False）+ `/api/chat` 200；公网 `app.js` 已含「网络不可用」降级徽章、`chat-window.js` 已含「本机开场白 · 未经大模型」）②国内备用 **https://qwer-d4gf2r76o8829463b-1458054906.tcloudbaseapp.com**（CloudBase 静态 + 云函数 chat，有效期至 2027-03-14；⚠️ 测试域名首访需点一次「确定访问」）；本地 fat jar 版同样可运行，**且 2026-09-26 r37 起有正式下载入口**：`https://github.com/lxh113377/xinyu-soulisle-private-archive/releases/tag/v1.4.1`（fat jar 28,438,588B + 零密钥前端包 249,409B/23 文件；远端实测 `isDraft=false`、2 资产；复算 `gh release view v1.4.1 --json isDraft,assets`） | 双线 curl 200✅；复算：`cd deploy && npx wrangler pages deploy xinyu --project-name=xinyu-soulisle --commit-dirty=true`（**必须 cd 进 deploy**，Functions 目录按 cwd 解析；部署日志须出现 `Uploading Functions bundle`，否则 `/api/chat` 会掉成 404）→ `python _test/live_sync_check.py` + `python _test/public_check.py`✅ |
| 4 | 官网报名信息 | 团队2-5人/指导教师≤2且非成员 | ⬜ 待老大填 → **待填清单已备**（报名信息-待填清单.md，2026-09-23） | 与PPT/方案口径一致（三处署名顺序必须相同） |

## 红线自查（提交前必跑）
> 已实测项标注取证时刻与命令；未实测项保留 ⬜，不得凭印象打勾。

- [x] 🔴 **密钥排除**（2026-09-23 实测）：`git grep -n -E "sk-[A-Za-z0-9]{20,}" HEAD` → **0 命中**。公网部署版 `deploy/xinyu/js/demo-config.js` 为 proxy stub（零密钥，实测 610 B、`KEY_LEAK=False`）；Key 仅存 Cloudflare Pages secret `DEEPSEEK_KEY`
- [x] 🔴 **成片重录的配置切换已按 sha256 还原**（2026-09-25 r30 实测）：`demo_video_pipeline.py` 自带守卫
  （要求同源代理、禁浏览器直连冒充在线），故录制前须临时把 `src/js/demo-config.js`（含 Key、已 gitignore）
  的 `base`+`key` 两行换成 `proxy: "/api/chat"`。**必须同源相对路径** —— 写成 `http://127.0.0.1:8123/api/chat`
  时页面在 `localhost:8123`，属跨源且服务端无 CORS 头（实测响应无 `Access-Control-Allow-Origin`），
  S3 直接落进「离线共情模板」而被断言拦下（第一次尝试 rc=1，如实记录）。
  还原核验：录制前后 `sha256=2521954c4f2087e330d0f3880dbf60eb0db03789f05a360bed14a670980be6d0` **逐字节一致**，
  且 `deploy/xinyu/js/demo-config.js`（公网零密钥 stub）未被触碰（`DEPLOY-SYNC-PASS` 红线复核在案）。
  录后复跑：`public_check` → `PUBLIC-ONLINE-ALL-PASS`、`live_sync` → `LIVE-SYNC-PASS`。
- [x] 盲审扫描（2026-09-24，AI 逐张读图 + 文档逐段核对）：**8 张截图与 PDF 均无校名/校徽/师生姓名/单位名/浏览器地址栏/书签栏/控制台**；PDF 封面署名为占位符。
  - ✅ 遗留 1 **已处置**（2026-09-24 二次核验后真相更正）：所谓「打码模糊块」实为**对话坞毛玻璃 `backdrop-filter` 透出身后幕标题的正常 UI 效果**，并非人工打码；静态 PDF 里观感确实像「藏了东西」。处置 = 重截：`s01` 改取滚动 +120px 构图（玻璃带后为空星空，第一幕标题完整）、`s08` 用当前版应用重截（含两次在线对话 + 「词典+LLM 分歧 → 采信 LLM」证据标签），已覆盖 `img/s01.png`、`s08.png` 并重渲染 PDF（18 页 / 8 图）。新脚本 `_test/screenshots_resubmit.py`（输出先落 `_shots/` 目检后才覆盖交付物）。
  - ✅ 遗留 2 **已处置**（2026-09-24，老大拍板"换图序"）：`s07`（在线双路分歧，词典 25% vs 大模型 85%）提为 **图 2 主探针图**；`s02`（离线徽章）移出主位，新增 **7.6 节「离线降级 —— 诚实标注模式」** 作示例图，图注写明"绝不以模板冒充在线大模型"，把劣势讲成诚信设计。重渲染后 **18 页**（≤20 ✅）。
  - 🔴 **新发现并已修复（2026-09-24）**：Edge `--print-to-pdf` 默认页眉/页脚把**本机文件路径**（`file:///C:/Users/.../项目/陪聊/...`）与打印日期印进了 PDF 每页页脚 —— 旧版 PDF 同样存在（盲审只逐张查了截图，漏了 PDF 页脚）。修复 = `render-pdf.ps1` 加 `--no-pdf-header-footer`；全 18 页文本复扫 `file://` 与本机用户名 **0 命中**。同脚本还修了「先删旧 PDF 再渲染、Edge 无害 stderr 在 EAP=Stop 下变终止错误 → 渲染中断且交付物已丢」的先毁后坏缺陷（改临时文件 + 成功后原子替换）。
- [x] 仿真数据标注（2026-09-24 实测）：`application-plan.html` 内「示例数据/演示数据/演示样例」标注共 **6 处**（含 7.4 情绪曲线"演示数据"角标、10.2 数据声明专节）；「调研」仅 2 处且均为**诚实声明**（"不代表本团队开展过同等规模的调研"/"未开展同等规模的一手调研"）；虚假声明模式（"调研实际用户"等）**0 命中**（输入非空已证：同文件其余 grep 均有命中）
- [x] **数字口径一致**（2026-09-23 校准）：情绪类别=6、叙事=5幕、评测样本 **73 条 / 98.6% / 危机召回 6-6**。⚠️ 旧值 **36 条 / 94.4%** 仍在多处历史条目里 —— 历史留痕保留原值 + 校正注，**但对外材料（PDF/视频/PPT）一律用新值**，三处必须相同
- [x] **赛道名称全文统一**（2026-09-23 实测）：`git grep` 命中 3 处，**逐条核对后判定均为正当描述**（"区别于主赛创新赛道…口径"、"省赛/区域赛是主赛创新赛道的三级赛制"是在描述**别的**赛道；第 3 处是清单自身的禁语行）→ 无违规，**不删规则本体以求绿**
- [x] 第三方模型如实声明（2026-09-24 随 PDF 产出完成）：第五章「开发工具」表内 DeepSeek 已标注"第三方服务"，并单列「第三方服务如实声明」框，写明"大模型能力来自第三方服务 DeepSeek，其余为核心自研"
