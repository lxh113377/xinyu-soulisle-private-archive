# 安全扫描裁定报告 — 心屿 SoulIsle（2026-09-24）

> 用途：裁决 2026-09-24 晨间被 Mimosa pre-commit 钩子拦截的 8 项 high（SSRF/代码注入），放行 7 个在制文件提交（老大裁决方案 A）。

## 1. 扫描执行记录（全部实测，含失败项）

| # | 通道 | 任务 | 结果 |
|---|---|---|---|
| 1 | 云端全量扫描（SAST，跳过SCA） | `1002648/1022606`（基线 main@96da855） | **canceled**（平台侧取消，0 命中不作结论）[报告页](https://qoder.com.cn/security-code-scan/reports/1002648/1022606) |
| 2 | 云端全量扫描补发 | `1002648/1022627`（基线 main@447a5e0） | **canceled**（同上）[报告页](https://qoder.com.cn/security-code-scan/reports/1002648/1022627) |
| 3 | 本地 L2 轻量评审（工作区改动=被拦 7 文件） | `review --layer=l2` | ✅ **findings_count: 0** |
| 4 | 本地 L3 深度评审（基线以来全部已提交变更） | `review --layer=l3` | ✅ **findings_count: 0** |

云端两次 canceled 判定为平台侧问题（非代码问题）；正式裁定依据取 #3/#4 双通道零命中 + 下表逐文件人工复核。

## 2. 原 8 项 high 逐条裁定（均为误报，复核依据）

| 命中文件 | 工具判读 | 实测事实 | 裁定 |
|---|---|---|---|
| `_test/browser_check.py` 等 ×5 | SSRF（外部 URL 请求） | 目标端点 = `127.0.0.1:18123`（**故意不可达**的本地回归端点，用于验证降级链路，见 `_test/j2_chat_contract.py` B 组设计） | 误报 |
| `deploy/cloudbase/functions/chat/index.js` | SSRF（base 可注入） | `base` 读自**服务端环境变量** `process.env.DEEPSEEK_BASE`（第 27 行），永不接受请求体输入 | 误报 |
| `_test/emotion_eval.js` / `_test/engine_lexicon_dump.js` | 代码注入（vm.runInContext） | 加载对象 = **本项目第一方引擎文件**（`src/js/emotion-engine.js`、`src/data/emotion-lexicon.js`），路径硬编码非用户输入 | 误报 |

## 3. 被放行提交的 7 个文件

`_test/screenshots_resubmit.py`、`交付物/提交包/render-pdf.ps1`、`交付物/提交包/img/s01.png`、`img/s08.png`、`心屿SoulIsle-应用方案.pdf`、`提交清单与验收状态.md`、`memory/2026-09-24.md`（日志段）——内容为 PDF 重渲染链路 + 盲审遗留处置，其安全性另有既有专项证据：Edge 页脚路径泄露已修（`--no-pdf-header-footer` + 全 18 页复扫 0 命中）、PDF 内 `file://`/本机用户名 0 命中。

## 4. 结论

**放行提交**。密钥红线复核随提交再跑：`public_check.py`（KEY_LEAK）+ `git grep sk-` 历史扫描。若后续云端扫描恢复可用，建议补一次全量扫描归档本目录以闭环（不阻塞本裁定）。
