# 2026-09-27 日志 · 卷1（r40d 收尾：ci_watch 两次自证 + 一次我自己又踩的管道坑）

## 一、看守工具在真面里跑了三次，每次暴露一件事

| 次序 | 事件 | 暴露的问题 | 处置 |
|---|---|---|---|
| 1 | `bash _test/push_and_watch.sh` 遇 `TLS connect error: unexpected eof` | push 本身失败 | 工具**没进 CI 判定也没假报通过**，直接 `PUSH-FAILED ⇒ 不进入 CI 判定` ⇒ 行为正确，重试一次即成功 |
| 2 | 推成功后 `ci_watch` 报 `UNVERIFIED \| 宽限 90s 后仍无 run 记录` | 90s 宽限在本网络太紧（run 登记可以晚于 90s），而实际那次 CI 后来是 **success** | 宽限默认 90s → **180s** 并开放 `--no-run-grace`；UNVERIFIED 文案补一句"登记更慢就加大宽限或稍后重跑"。**方向要说清**：它是 fail-safe（不误报绿），但总哭狼的工具会被人关掉 ⇒ 校准而不是放宽判据语义 |
| 3 | 同一次运行我写了 `python ci_watch.py \| tail -6; echo $?` 拿到 `rc=0` | **管道的退出码是 `tail` 的**，被测对象的结论被吃掉（本仓已登记过的老坑，我又踩了一次） | 改"重定向到文件后取 `$?`"⇒ 实测 `real_rc=0` 且首行 `CI-WATCH-GREEN \| 4a197ca4`；这次是**先看到假 rc 再回查**，不是先想到 |

## 二、本轮受理面

- 项目 `4a197ca` CI **success**（54 套件 = 实跑 51 + 豁免 3；本地 `26/26 + 25/25`）；
- `HEAD == ls-remote`（三仓）；
- 交付面判据首跑真面：`PLAN-PDF-COVERAGE-PASS: 官方九项全覆盖（9/9）｜页数 20 ≤ 20｜分母取自大纲`。

## 三、还剩什么

- 老大侧：09-30 前官网报名 + 提交（PII 只有你能给）、fat jar 目标机器、`_test/cors_probe.py` 里那把 key 建议轮换。
- 下一刀可做：`08-ac-obs.md` 的 AC 追溯键**存量重编号**（07 卷32 P2，7 个 id 各挂两条命题），
  G15 已把增量钉住，存量要挑干净窗口动，因为交叉引用散在 README/05/07 多卷。
