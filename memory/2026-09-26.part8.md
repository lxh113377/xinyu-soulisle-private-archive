# 2026-09-26 会话日志 · 卷8（r38 · 晚间轮：安全与评测能力面 → 护栏 + CI 前置复现）

> 同条对标指令第三次重发。台账第三轮**零实质漂移** ⇒ 继续换面：这次量"产品到底做了什么"。

## 一、新面实测（16/16 取到，无 NA 无截断；两通道 = 文件树 + README）

危机转介 树 1/16 · README 1/16（我方 README 有、文件名看不见）；
可复跑评测集 树 3/16 · README 2/16（我方两通道都有：73 条 + 双端全等）；
**内容防护 树 5/16 · README 1/16，我方两条都是 0** ⇒ 唯一真差距，本轮只补它。
CI 徽章 9/16、外链 14/16（我方已在）；README 演示动图 **0/16** ⇒ 行业都没做，不跟进。

探针自身修过两处：判据收紧（早期会把 `Array.filter`、Java 依赖注入判成"安全件"）；
加对称排除 —— 文件名含 `probe|check|scan|verify` 的**工具自身不计入能力**，
因为我写的安全探针文件名带 safety，不排除就给自己凭空加一格
（r35"检测密钥的判据是全仓最像密钥的文件"同族，这次抓的是我自己）。

## 二、护栏怎么落的（边界比功能更重要）

`SafetyGuard`：8 类模式（指令覆盖中英、提示词套取含中文「把…输出」倒装、角色伪造、persona 越狱、
编码载荷）+ 单轮 >4000 字截断 + 命中即向上行 messages 追加系统重申。
**响应体一字不改** —— 契约是"上游逐字透传"，流式改写必须整段缓冲＝废掉流式；
判定只落响应头 `X-Xinyu-Safety`，输出侧只做分类，最高危自伤由危机层兜底（三层写进 README）。

判据 `safety_guard_check`（电池 45→**47**）走 HTTP 双向：注入 6 例必须点对应类别、正常 6 例（含 2 例
"近似误伤"：课文复述／汇报输出）必须判 0、危机句不误伤、致死句 risk=high、超长 capped、流式同带头。
**它当轮就抓到我的第一条真漏报**：中文「把…提示词…输出」是名词前置，原式只认动词前置。
