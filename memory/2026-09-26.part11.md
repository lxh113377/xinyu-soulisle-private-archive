# 2026-09-26 会话日志 · 卷11（r39 续卷：工具缺陷登记与自我误判撤回，自卷10 按 4KB 同尺续拆）

## 四、两条真实缺陷（登记不代改）＋一条我自己撤回的误判

- `trim-shell` 追加写盘**产出 CRLF**：part27 `2,634B/CR=0 → 3,929B/CR=46`，`eol_parity` 当场判红
  ⇒ 与 r36 那条红线同源（`write_text` 文本模式）。本地按字节还原，6 条迁移项完好。
- `handoff.py help <在册命令>` 回 `Unknown command`，而 `trim-shell`/`volume`/`recycle` 都在分发表里
  （`handoff.py:174-177`）⇒ "先跑 help 现读"这条护栏会**给假阴性**。
- 我一度把上面两条合并判成"体量判据的处置指引指向死命令"，准备按高优先缺陷落进 skill；
  核实分发表后**撤回**（命令在册，我第一次只看了 `--help` 前 25 行）。
  没写进任何文档，但记下来：**拿被截断的视图当全集**是本项目登记过的老形态，这次轮到我看帮助列表时又犯。

## 五、下一步（本轮新增，都在 part29/part30）

- [ ] P2 修 `help <cmd>` 主题表（属主侧，附复现命令）
- [ ] P2 `cmd_trim_shell` 写盘改按字节（属主侧，附复现命令）
- [ ] P2 AGENTS.md 注入超量需**人工裁口径**（不删内容、不强推重生成）
- [ ] P3 `_trash/` 现堆 81MB，超期处置走工具链（>7 天进判据），不手删
