# 2026-09-26 会话日志 · 卷5（r36 续卷：按 4KB 同尺自卷4 续拆，条目逐字未改）

## 五、公网同源：把"内容等价"的容差换成真相等

`live_sync_check` 从 `live=9463 / local=9288｜仅行尾差异 ⇒ PASS`（靠 `eol_only` 兜底）
变成 `live=9288 == local=9288 equal=True`。
部署命令取在册那条（`07…part18`），不凭记忆裸跑：`cd deploy` 是硬前提（Functions 目录按 cwd 解析，
日志须见 `Uploading Functions bundle`）。回滚锚点 = 上一版生产 `b5f46ecf-d1dd-46ee-b700-10a3d2914e56`。
四件套复验：`public_check`（KEY_LEAK False / console 0 报错）、`online_check`、
`offline_shell_check`（17 项 0 失败）、`deploy_sync_check` 全绿。
`eol_only` 分支**保留**（CDN/检出差异需要容差），但它不再是"线上==权威源"的支撑。

## 六、对标补测与一条口径纪律

16 仓根目录 `.gitattributes` 逐个探测 ⇒ **存在 6/16、真钉行尾/二进制 4/16**（lobehub / leemo /
opensoul / MoodChat，后者钉的是 `eol=crlf`）。
第一版探测用 shell `base64 -d`，读出一张 **16/16 全部存在、全为 0 字节**的假表 ——
解码静默失败被当成"内容为空"，与"404 取不到"两种事实混在一起。改成 `gh api` 按 HTTP 状态分档 +
Python 解码后才拿到真数。⇒ 结论也从"人人都钉，我没钉"改成"这是少数派做法（4/16），但与工程化
天花板同形"，优先级据此定，而不是按"行业默认"给自己加压。

## 七、台账与复跑（数字都取当场打印）

- 台账重采：`BENCHMARK-METRICS-PASS`，`[self] … 电池套件=43(判据脚本文件=26，两口径不同源即登记)`，
  漂移 3 处 = 实质 0 + 抖动 3（★ 单点差值不作趋势证据），`ts=2026-09-26 09:31 UTC`。
- 本机全量：`--slice 0 22` ⇒ **22/22 rc=0 ALL-GREEN**；`--slice 22 43` ⇒ **21/21 rc=0 ALL-GREEN**。
- 新增 **G14**（README 现行状态句「对标源数据台账（N 仓指标」== 台账 `peers_expected`）：
  起因是本轮改 README 时看见它写着"14 仓"而池子已是 16。`--selftest` 40 条断言（含 ⑰a–⑰d 四向）。
- 提交序列：`13a7299`（归一+判据）→ `e45ba62`（电池入口）→ `c3f8c1a`（同类写盘口）→ `09ddc55`（文档+G14）。

## 八、遗留 / 只上报不清理

- `eol_parity` 分母是 `git ls-files` ⇒ 新建未 add 的文件本地不被扫（提交后 CI 才扫到）。
  本轮对三个新卷手工核验 `CR=0` 顶住窗口，判据改造登记为低优先（报告 §10.7）。
- 待老大：iCAN 报名 PII（硬截止 2026-09-30）、fat jar 目标机器、`_test/cors_probe.py` 里那把
  已入 ignore 的 Key 轮换。
