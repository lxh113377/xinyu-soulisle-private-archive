# 2026-09-24 会话日志（提交包盲审遗留处置）

【数据流假设】
来源: 提交包截图 `交付物/提交包/img/s01.png`/`s08.png` 与 `iCAN评审/截图/01/07` 同哈希（SHA256 实测）；页面 = fat jar(8123) 托管 `src/`（`/api/health` 实测 webRoot=src）；在线 LLM = `src/js/demo-config.js` 预置 Key 浏览器直连。
流向: 新脚本 `_test/screenshots_resubmit.py` → 先落 `_test/_shots/` 候选图 → 目检通过 → 覆盖 `交付物/提交包/img/s01.png`(滚动120px候选)、`s08.png` → `render-pdf.ps1` 重渲染 PDF（临时文件→校验→原子替换）。
结构: PNG 1280×800 viewport；PDF 判据 = 页数≤20 / 图片=8 / 全文 `file://`·`37533` 0 命中。
异常: LLM 超时/离线徽章 → 脚本断言 ONLINE-EVIDENCE 拦截不覆盖；渲染中断 → 旧 PDF 保留（修复了先删后渲的先毁后坏缺陷）；页数>20 → 告警。

## 本轮处置
1. **盲审遗留 1 关闭**：「打码模糊块」实为对话坞毛玻璃透出幕标题的 UI 效果（非人工打码）——s01 重截换构图、s08 重截当前版（双路证据标签在图），PDF 18 页 / 8 图复验。
2. **新发现+修复**：Edge 默认打印页脚泄露本机文件路径（旧版 PDF 即存在）→ `--no-pdf-header-footer` + 全 18 页复扫 0 命中；同修 `render-pdf.ps1` 先毁后坏（EAP=Stop 下 `2>&1` 把 Edge stderr 变终止错误）→ Start-Process 重定向 + 临时文件原子替换。
3. **仿真数据标注红线转 [x]**：HTML 内标注 6 处、调研仅 2 处诚实声明、虚假声明 0 命中（输入非空已证）。
4. 回归：`deploy_sync_check` PASS（三类归零 + 零密钥复核）；`browser_check` ALL-ASSERT-PASS（4 条 CONNECTION_REFUSED 为脚本自注入不可达端点已知噪音）。

## 遗留（需老大）
- **git 提交被拦截待裁决**：新装的 Mimosa pre-commit 钩子报 8 个 high（SSRF/代码注入），逐条核实**全为存量文件误报**（①`_test/*.py`×5 = 打 127.0.0.1 的本地回归脚本；②云函数 `chat/index.js` 的 `base` 来自服务端 env `DEEPSEEK_BASE` 非用户输入；③`emotion_eval.js`/`engine_lexicon_dump.js` = `vm.runInContext` 加载本项目第一方引擎文件）。7 个文件已暂存未提交；**未擅自 --no-verify**，等老大决定（放行 / 走 Mimosa 深扫出正式报告 / 标记误报机制）。
- 演示视频 MP4 需真人录制；官网报名信息待填（`报名信息-待填清单.md`）；fat jar 国内目标机器未定。

## 收尾（02:40）
- R199 拆卷：part6 单块超限（整列表项粒度无法自动拆）→ 人工拆出 part11（fat jar 部署史，内容零改写）+ 索引壳登记。
- noise 门禁：清除 `D:\global_skills\_bm_skillid_migration.json` 散落（迁 _trash，非本会话产物）→ `[GATE:noise-pass]`；savepoint 完成。
- 经验沉淀：`lessons.part40.md` 两条（Edge 页脚路径泄露 / 先删后渲+EAP=Stop）+ GM 日志 footer 锚点 `[GATE:evolution-pass]`。


---

## 对标轮第二轮（17:20–18:10，QD 端，自动化执行授权）

### 前置核验（R-CURRENT）
- 发现首轮报告 `交付物/对标分析报告-2026-09-24.md` 已存在（同日上午 96da855）→ **不重复创作**，改为「实测复核 + 扩容 + 把首轮推后的项落地」。
- 首轮数字全部重拉：LobeChat 82,794→**82,798**、OLV `pushed=2026-05-15`（确认 4 月停更）、SillyTavern 33,733/1.19.0(09-14)。
- `gh` CLI 与网络**当场实测可用**（v2.95.0，keyring 已登录，直连 `api.github.com` 200）→ 与 skill 内"gh 是坏 shim"的历史记载**冲突，以实测为准**。
- 22 组 `search/repositories` 查询 → 去重 **211 仓**候选池；挑 14 仓拉 `/repos`+`/languages`+`/releases`+`/actions/workflows`+`/git/trees` 深指标（脚本与 JSON 落 `%LOCALAPPDATA%\Temp\bench\`）。

### 本轮落地（M1–M8）
一句话：**首轮把 M1–M6 全部写成"赛后再做"，本轮全部当场做完并配判据。**
- M1 流式 SSE（前端 `onDelta` + Pages Function + Java）；M2 共情策略表 SSOT；M3 `LLM_*` provider 别名 + 5 家预设；M4 TTS 朗读；M5 对话窗口化；M6 响应式三档 + 粒子降档；M7 CI 三门禁（词表一致性红线终于机器化）；M8 `ROADMAP.md` + `pom 1.3.0` + CHANGELOG + tag `v1.3.0`。

### 实测踩坑（四条，全部当场定位并修）
1. **Spring `ResponseEntity<?>` 装不下 StreamingResponseBody** → 报 `No converter for [...Lambda...] with preset Content-Type 'text/event-stream'` → HTTP 500。判据 B 首跑直接暴露。修法：整个方法签名统一 `ResponseEntity<StreamingResponseBody>`，整包 JSON 也走同一出口。
2. **CI 密钥扫描自伤**：workflow 里写死的对照密钥 `sk-01234…` 被自己的扫描命中（本机实跑 `git ls-files | xargs grep` → `HITS=1` 抓到）。修法：对照组密钥改**拼接生成**（`printf 'a%.0s' $(seq 1 30)`）。→ 印证 R236「门禁命令必须先本机实跑再写进文档」。
3. **「展开较早」形同虚设**：放回 20 条立刻被同一条 `RENDER_MAX` 裁回（实测 `60 -> 60`）。修法：改**配额制**，展开临时抬高配额、新消息收回。
4. **判据自己写错**：断言 `getHistory() >= 90` 失败——`chat-agent.js` 既存 `history` 上限就是 40（`slice(-40)`），与窗口化无关。修法：改判据为"等于既存上限"+ 另加 `MemoryStore == 45 条` 双证，**不为了变绿而改数据**。

### 判据结果（全绿）
`browser_check` ALL-ASSERT-PASS（LIT 0/5/17/17/0 不变）· `j2 J2-CONTRACT-PASS` · `engine_consistency` 98.6% / 6-6 双端逐项全等 · `pixel LIT-COLOR-CHECK-PASS` · `lightshow PASS` · `j4 J4-MEMORY-PASS` · `fuse J4-FUSE-PASS` · `voice VOICE-PASS` · `strategy PASS + selftest PASS` · `stream A/B/C PASS`（32 帧/30 内容帧） · `ux_guards 21/21` · `deploy_sync 三类归零` · `mvn BUILD SUCCESS`

### 提交与备份
- `6cae042` 代码簇（pathspec 精确到 22 个文件，**未裹入老大的 7 个在途暂存文件**）→ `git_autopush --push` SHA 核对一致。
- 第二簇（文档/CI/记忆/报告 + 新增判据脚本）随后提交；tag `v1.3.0`。

### 遗留（需老大，一句话即可推进）
1. 🔴 **公网部署**：`deploy/` 已同步但线上仍旧版 → `npx wrangler pages deploy deploy/xinyu --project-name xinyu-soulisle` + `public_check`。外发变更，未代跑。
2. iCAN 三项缺件（PDF 解冻一句话 / 演示视频真人录 / 报名表填写）不变。
3. 真机（iOS Safari / Android）布局与帧率未测。

### 补记（18:20）· CI 真跑抓到本机不可见的缺陷并已修
- 现象：GitHub `browser-regression` job FAIL —— `AssertionError: console 报错: [404, 404, 404]`，而同 job 里 `WEBGL_ACTIVE: True`、`LIT: 0/5/17/17/0` 全对（SwiftShader 方案有效）。
- 根因：`src/js/demo-config.js` 被 `.gitignore` 排除（内含本机真实 Key），但 `index.html` 用 `<script src="js/demo-config.js">` **静态引用**它 ⇒ 全新 clone + fat jar 首屏必然 3 个 404，打破「console 0 报错」判据。**本机永远看不到**（本地该文件一直在）。
- 处置：CI 新增步骤用 `deploy/xinyu/js/demo-config.js`（零密钥自适应 stub）补占位 + 复制后二次扫密钥；`CONTRIBUTING.md`「环境要求」首行写清全新 clone 第一步；README 验证段记录该缺陷。
- 本机忠实复现（非猜测）：临时把 stub 放进 `src/js/` → `browser_check` **ALL-ASSERT-PASS 且零 404**；随后按 SHA 还原真实文件（`e814bd38…` 前后一致）并复跑 `deploy_sync_check` 三类归零 + 红线复核（stub 与 src 不同 = True）。

## QD 对标轮（Qoder 会话，18:30 起）
- 产出：v1 对标报告（三仓七维）+ 工程七件套（96da855/0638513 等，后与并行对标轮 v2 合流，v1 报告已补更正注 e93fd00）。
- 老大四裁决落地：①A 云端全量扫描**结果 canceled**（0 命中不作"干净"结论；7 个在制文件提交仍挂起，等重扫指示）②官方视频要求复核=**无人声强制**→建 `_test/demo_video_pipeline.py` 自动产片（8幕真链路+断言+字幕烧录+edge-tts）③报名 5 人名单落盘，**仍缺**学号/手机/邮箱+指导教师+官网登录态 ④fat jar 部署**先不办**，降级 10 月复赛节点。
- 附带：清 `%SystemDrive%/` 垃圾目录（回收站台账可还原）；踩坑——headless-shell 缺失须回退 msedge、管道 `| tail` 吞 exit code、流式下"新消息出现≠终稿落定"须等 data-emotion。
- run2-5 五连败真根因（分两层）：① run2/3 = 本机浏览器直连 DeepSeek 挂死（curl 1.6s 通，浏览器路径不通，瞬时环境问题）→ 录制改走 fat jar 同源代理（实测 0.58s，零密钥前端，恰为生产形态）；② run4/5 = **判据竞态**：`.msg.ai` 基线在 click 之后才取，jar+deepseek-flash <1s 即回，回复被算进基线 → 计数恒不增 → 假"无回复"。修复=基线先于点击（S3 与 send 两处）。教训：**测"新消息到达"禁用事后基线计数**，与 R253「测错对象=假通过」同族。A/B 单变量法（录像开/关同挂）是定位到"环境无罪、脚本有罪"的关键一步。
- run6-7 后成片：217s / 22.4MB / 8 幕断言全绿 / 字幕烧录 + edge-tts 中文旁白（mean -25dB 非静音）。装配段连踩三坑（均已修进流水线）：ffmpeg subtitles 滤镜中文绝对路径判坏（改 OUTDIR 内相对路径）、TTS 混音 -map 0:v 指到旁白 mp3（改末位输入索引）、ASS Dialogue 列数与 Format 不符致表头漏字。录制形态定版：**fat jar 同源代理**（浏览器直连 DeepSeek 本机已证不稳，禁回退）。
- ①A 闭环：云端两次 canceled（1022606/1022627）→ 本地 L2(工作区)+L3(已提交) 双通道 0 命中 + 8 项 high 逐条误报复核（见 交付物/安全扫描裁定报告-2026-09-24.md）→ 7 个在制文件放行提交（4b48119），密钥 HEAD/暂存双扫 0 命中。`%SystemDrive%/` 系并行工具反复重生，改走 gitignore 豁免（R269）。

## r14 全量执行轮（QD，23:5x）
- PWA 半件落地（manifest+icon，无 SW）：`0da94d8`；deploy_sync 三类归零 + browser/pixel 双 rc=0。
- **回归环境口径定版**：J4 本地层 `remote:true` 下，http.server 形态必现 `/api/memory/emotions` 404 → 全套回归/演示的正确形态 = **fat jar@8123**（DEEPSEEK_KEY 走进程环境、webRoot 默认 ./src）。jar 现由本会话拉起在跑。
