# 2026-09-25 会话日志

## r18 全量执行轮（QD 对标轮，00:0x）
- 对标源数据重拉（gh api 实测）：**lobehub/lobe-chat 已改名 `lobehub/lobehub`**（301，★82,800，活跃）；OLV ★13,899（pushed 2026-05-15，停更结论不变）；ST ★33,739（活跃）。七维判定无翻牌。
- 改进项落地 = 把 r17 的公网收口**固化为机器守卫**：`_test/live_sync_check.py`（线上 `/` 与 `deploy/xinyu/index.html` 逐字节比对 + 被引 js/css 缺失即红；rc 0/1/2 三态分离"漂移"与"环境不可达"）。反例对照：指 example.com → rc=1，判据非恒真。
- 接入执行路径：`run_all_suites.py` 16→17 套件，实跑 **17/17 rc=0 ALL-GREEN**；`ci.yml` 新增 `live-sync` job（仅 main push、continue-on-error 告警不阻塞，YAML 校验 OK）。
- 报告追加 r18 段；07 part12 勾销行补守卫指针。

【数据流假设】
来源: `curl api.github.com/repos/*` 三仓实测 JSON（含 301 重定向证据）+ 线上 `/` 与本地 `deploy/xinyu/index.html` 各 8708B 字节相等实测 + example.com 反例 rc=1 + `run_all_suites.py` 17 条逐项 rc
流向: 对标数据刷新 → 识别可执行改进（部署新鲜度守卫）→ 新建套件 → 正反例自证 → 挂电池 + CI → 报告 r18 段/日志 → 逐路径 add/commit/push（SHA 双核对）→ savepoint + GM 块
结构: UTF-8 无 BOM；rc 三态约定（0 过 / 1 漂移 / 2 环境）写入脚本 docstring；CI job 仅 push main 触发且不阻塞合并流
异常: 日期已翻至 09-25 → 本文件为新日志（09-24 卷冻结不再追加）；`memory/sessions/repeat-guard.jsonl` 为 cron 投递侧在途件，按 R269 只登记不擅动；管道取 rc 必须 `${PIPESTATUS[0]}`（本轮 stale-probe 一度被 `tail` 吞码，即前日同族坑二次踩中）

## r19 全量执行轮（QD 对标轮，00:3x）
- 数据刷新两条新信息：LobeHub 定位改写为 Agent 编排平台（聊天前端参照价值不变）；新增第四参照 letta-ai/letta（stateful agent 记忆，★24,867）为 J4 记忆叙事的同构先例。
- §2.3 性能差距对账：虚拟化/响应式/PWA 三项确认已收口（U2/U3/r14 决策），唯一残余可执行项 = **体积预算守卫** `_test/size_budget_check.py`（13 文件+总预算、selftest 压 90%→14 项报红证非恒真、demo-config 缺席 WARN 分支）。电池 19/19 全绿，CI frontend-checks 增步。

## r20 全量执行轮（QD 对标轮，05:3x–06:0x）
- 对标源数据首次**机器化**：`_test/benchmark_metrics.py` → `交付物/对标数据/benchmark-metrics.json`（14/14 仓采集成功 +
  self 指标同产物），每次跑输出与上次快照逐字段漂移；能力探测从「根目录一层」改 **`git/trees?recursive=1` 整树**
  （根层探测无判别力：14 仓 sw/locales 全在子目录，只看根一律报"无" = 把"没测到"当"没有"，M4 失效）。
- 实测覆盖率：vector_memory 8/14、container 5/14、pwa_manifest 4/14、i18n 3/14、e2e 3/14、
  deps_autoupdate 2/14、streaming 2/14、**pwa_offline 0/14** ⇒ **撤销 v2/ROADMAP 的"SW 排赛后第一位"**（前提未经核实）。
- 高优先落地：① `/api/emotion` 前端接线（07 P0 挂账项，`src/js/emotion-remote.js` + 判据 9 项 + selftest 3 类篡改全抓）
  ② vendor 供给链守卫（`vendor-manifest.json` + V1 完整性/V2 内容解析版本/V3 漏登记/V4 上游漂移）
  ③ gsap+ScrollTrigger 3.12.5→3.15.0 成对升级（三套件 rc=0）④ 体积预算 coverage 补洞（实测抓到漏登记 emotion-strategy.js 4,720B）。
- 公网跟进：deployment `208e1743`；`live_sync`/`public_check`/`online_check` rc=0。版本 1.4.0（pom 1.3.0→1.4.0，fat jar 29,419,279B）。
- **本轮踩坑两条（必须留痕）**：
  1. `mvn package` 在 **jar 进程存活时** 跑 → `repackage` 无法把 `soulisle-server.jar` 改名为 `.original`（Windows 文件锁），
     构建 **BUILD FAILURE 且把磁盘上的 fat jar 降级成 48,920B 瘦 jar**（进程仍在内存里跑，健康检查照常 200 ⇒ **假健康**）。
     修法：先 Stop-Process 占用 8123 的 java → 再 package → 29.4MB fat jar 恢复 → 重启后 `/api/health` UP、`/api/emotion` 双路正常。
     **判据：改 pom/构建 fat jar 前必须先停占用进程；构建后必须 `ls -l` 看体积而不是只看 curl**。
  2. `run_all_suites.py` 里 `sys.stdout = io.TextIOWrapper(sys.stdout.buffer)` 新建的 writer **默认行缓冲关闭** ⇒
     重定向到文件时**运行期一直 0 字节**，退出才落盘。误判为"卡死"两次并 kill 了一轮有效数据。
     **判据：后台长跑判据脚本要 `python -u` 且不得据此判断进度，完成通知才算结论**。
- 回归环境：jar@8123 已换新构建（1.4.0）；`src/js/demo-config.js`（含 Key，gitignore）置 `emotionRemote: true`，
  公网 `deploy/xinyu/js/demo-config.js` 刻意不含 ⇒ 公网评委零 404、零 console error。

【数据流假设】
来源: `curl` 实测 jar 的 `POST /api/emotion` 返回形状 `{lex{crisis,emotion,intensity,all,color},llm,final,path}` + `src/js/chat-agent.js:171-212`（classifyEmotion/respond 实读）+ `src/js/memory-store.js:22-34` 三层开关先例 + 上游 `gh api repos/{mrdoob/three.js/releases/latest,greensock/GSAP/tags}` 实测 r186 / 3.15.0 + `grep -o '[a-zA-Z$_]="1[0-9][0-9]"' src/vendor/three.min.js` 唯一命中 `e="128"`
流向: `src/index.html` 引 `js/emotion-remote.js`（engine 之后、agent 之前）→ `chat-agent.respond` 经 `EmotionRemote.classifyWithBackend(text, 本地classifyEmotion)` → 上层 app.js 消费同一形状对象（字段零改名，仅新增 `emoSrc`）→ 星图取色仍走本地 `EmotionEngine.colorOf`（不破像素级标定）→ MemoryStore.record 不变 → 同步 `deploy/xinyu/`（零密钥零 emotionRemote）→ Pages 上线
结构: 后端响应 lex.all = [{emotion,score}...] 与 JS 侧同构；UTF-8 无 BOM；三层开关口径与 J4 逐字对齐；vendor-manifest 键名（file/version/detect/sha256/size/status/upstream_seen）与脚本常量一一对应；rc 三态约定 0 过 / 1 判据失败 / 2 环境异常
异常: /api/emotion 404·网络失败·超时 4s·形状不合法 → `markDown()` 熔断 + 回落本地 + 界面不出现「情绪:后端」（W5 实测）；危机词在 isRemote 判定之前短路，后端独知危机时采纳并计 overridden；vendor 版本正则零命中即判红（禁"没测到=通过"）；jar 被进程锁住时构建必失败 → 先停进程；manifest 与磁盘文件集合不一致 → 覆盖判据报红

### savepoint 被拒的真实归因（不是本仓问题，禁"清理他人文件以求绿"）
`handoff.py savepoint` 的 **步骤 i 噪声散射门禁扫的是四个受管根**（global_skills / global_memory /
global_memory_archive / 焚诀），报出的 5 条违规**全部不在本仓**：
`D:\global_memory\.git/push-failure.log`、`焚诀/{CONTRIBUTING.md, LICENSE.md, SECURITY.md, .git/push-failure.log}`。
- 处置：**不迁不删他人仓的文件**（R269：活跃工具态改走 .gitignore 豁免；他人项目文档可能是有意放置）；
  本仓 a–h 步骤（P0 非空 / 05 进度 / sanitize / 绑定表保护）均正常通过，实质交接已完成并已提交推送。
- **登记为治理缺陷（需老大拍板，非本轮擅自改）**：门禁作用域**范围泄漏** —— 一个项目的 savepoint 被另外两个
  仓的历史散落项挡住，会诱发"为变绿而清理他人文件"的危险动作（正相反，正确动作是**缩小门禁作用域**：
  噪声判据应只针对**目标项目自身根**，跨根部分降级为报告项）。修复位 = A-project-handoff `handoff.py` 的
  cmd_noise 调用面 + `noise_lint` 双副本 allowlist（R209 同源纪律）。

## r21 全量执行轮（QD 对标轮，同日第二轮，06:3x–07:0x）
- 用户命令重发 ⇒ 按原文完整重执行（非"已核验"降格）。刷新数据 → 找差距 → 执行 → 配判据 → 回归 → 回写 → 提交推送。
- 高优先落地：① `docs/openapi.yaml`（11 条接口，全部实读控制器与 `LlmProxy` 取得，未凭记忆）+ `_test/api_contract_check.py`
  五判据（C1 缺文档 / C2 幽灵 / C3 前端偷调 / C4 运行态状态码与必需键 / C5 自证非恒真），入电池 24→26 与 CI `java-build`；
  ② `.github/dependabot.yml`（maven + github-actions）——**同时纠正 r20 自己的错误归因**"零构建挂不上 dependabot"，
  06 原条目保留并加更正注。
- 测量装置自纠两处：漂移纳入 caps/docs（实测 sapphire 掉 container 却零报告）；self 套件数改以 `run_all_suites.py` 条目为唯一分母
  （此前 19 文件 vs 报告 24 套件是两个口径混用）。参照池 14→16（新捞 ryza-ai-revive 190★活跃、MoodChat Java 同栈）。
- 回归：`api_contract` 10/10、全量电池 **26/26 rc=0 ALL-GREEN**；self 账面 docs 8/9→**9/9**、能力位 `api_spec`/`deps_autoupdate` 各 0→1。
- 判据两处真修：`urllib` 传小写 method 打到 Spring 得 **405**（`Request(method=...)` 原样带上线路）；
  `live` 字典键用 `verb.upper()` 而 order 列表存小写 ⇒ 探测 0 条被 `C4 覆盖度` 判据抓到并报红（**判据先把我的错报出来了**，非假通过）。
- 并行会话观测：8123 的 java 进程被外部重启（PID 36932→30596）⇒ 本 worktree 仍有其他会话活动，状态断言前重测端口、提交 pathspec 限定。

【数据流假设】
来源: 实读 `server/src/main/java/com/xinyu/soulisle/api/{Chat,Emotion,Health,Memory}Controller.java`（grep `@*Mapping` 抽 11 条路由与错误体，`LlmProxy.java:79-116` 抽 upstream-nonjson/upstream-error 502）+ 实跑 `POST /api/emotion`、`/api/health`、`gh api search/repositories` 两查询 + `python _test/benchmark_metrics.py` 三次采集（16/16、漂移 1 处 SillyTavern ★、历史 6 次快照）
流向: `docs/openapi.yaml` 成为接口唯一声明源 → `api_contract_check.py` 三方对账（控制器 ↔ spec ↔ 前端 src/js）→ 运行态真实打 8 端点 → 结果入 `run_all_suites.py`(26) 与 CI `java-build` → 报告 §9 / 05-08 记忆 / CHANGELOG / README 回写 → pathspec 提交 + push + SHA 核对
结构: OpenAPI 3.0.3，路径含 Spring 风格 `{sessionId}`；自定义键 `x-live-check: {method, body, query, status[], required_keys[]}`；守卫内 `norm()` 把 `{var}` 归一为 `{}` 做三方对齐；dependabot `version:2` + 两个 `package-ecosystem` 条目，directory 分别 `/server` 与 `/`（实测两目录均存在）
异常: jar 不可达 → rc=2（不把环境故障当通过）；spec 缺 PyYAML → rc=2；C4 探测写完必 DELETE `contract-probe` 清理，避免污染评委数据；dependabot 目录不存在 → 配置无效（故先实测目录存在再写）；小写 method 已修并记入 lessons（405 属"判据读数失真"类）；并行会话改端口 → 任何服务断言前重新 `netstat` 实测

## r22 全量执行轮（QD 对标轮，同日第三轮，07:3x–07:5x）
- 数据侧**零漂移**（16 仓逐字段与 r21 相同）⇒ 本轮差距全部来自复审上一轮我自己的交付物，不编造外部差距。
- 抓到并修两处真缺陷：
  ① **C4 阈值掺水**：上一轮写 `done >= 8`，而 spec 真实操作 11 条 ⇒ 3 条 memory 读接口既不探测也不解释仍判绿。
    补 `docs/openapi.yaml` 三条 `x-live-check`（探测 8→11 全覆盖）+ 新增 C6 `探测+豁免==操作总数` + C6b 零豁免；
    `--selftest` 加第 5 类样本（抹掉标注必须被抓）。
  ② **「配了 dependabot」被当成「生效」**：GitHub 只读默认分支、ecosystem/directory 拼错静默不跑；
    且文档数字断言（26 套件 / 四条门禁）无机器责任 ⇒ 新增 `_test/repo_config_check.py` G1–G5 + selftest。
- **新守卫当轮即抓到真实脱节（红→绿留证）**：加 2 条套件后 README 仍写 26，G4 报「实测 28、声称 26」，改文档转绿。
- 电池 26 → **28**；07 下一件仍是 `app.js` 469 行切分（附新前置：加文件必须同步登记 `size_budget` 表，否则 coverage 拦）。
- 回归（**最终状态**，分两段前台复跑）：`--slice 0 14` → 14/14 rc=0；`--slice 14 28` → 14/14 rc=0 ⇒ **28/28 全绿**；
  `api_contract` 14/14（探测 8→11 全覆盖 + C6/C6b）、`repo_config --online` 5/5（G5 默认分支可见、G6/G7 授权边界）。
- **自审第三条发现**：整仓声明 MIT，但 `src/vendor/gsap.min.js` / `ScrollTrigger.min.js` 实为 GreenSock Standard
  License（文件头 banner 自证，非 MIT；只有 three.js 是 MIT）⇒ 新增 `docs/THIRD-PARTY-NOTICES.md` 逐文件声明 +
  README 中英口径改「MIT 仅覆盖自研代码」，并做成判据 **G6**（vendor 每文件必须在清单点名）/ **G7**（README 必须引用清单），
  `--selftest` 两类新反例（抹 gsap 行、抹 README 引用行）均被抓到 ⇒ 「六类合成篡改全抓」。
- **自己刚写的开关是死代码（已修 + 留证）**：`--slice` 第一稿先滤掉 `--` 参数再判 `args[0]=="--slice"` ⇒ 永不命中，
  `--slice 0 14` 实际跑了 28 条还报全绿。修法：按 `sys.argv` 原样解析 + `--list` 打印过滤后条数 + **零套件直接 rc=1**
  （禁止 0/0 当通过）。实证：`--slice 3 3` → 0 条 → FAIL rc=1；`--only repo_config` → 2 条。
  另：`28/28` 一度在证据到手前写进报告（两次后台运行输出文件 0 字节），已撤回并前台复跑后才写回 ⇒ **后台 exit 0 ≠ 产物存在**。
- **同族第三次复发（改文档正文的引号坑）**：用 shell 内联 python 改中文带引号文本，两次 `SyntaxError`
  （双引号嵌双引号），第二次才改用编辑工具直改 ⇒ 教训：**改正文优先结构化编辑，不借内联脚本传中文引号文本**。

【数据流假设】
来源: `docs/openapi.yaml`（本轮 python yaml 解析实测：操作 11 条 / 标注 8 → 补至 11）+ `grep -c x-live-check`（9 处含 1 处正文提及，据此定位漏标）+ `run_all_suites.py` SUITES 正则计数（26→28）+ `gh api repos/{o}/{r}/contents/.github/dependabot.yml`（默认分支可见实测）+ `README.md` 正则抽声称值（门禁 4 / 套件 26）
流向: 复审上一轮守卫 → 补 spec 标注 → `api_contract_check.py` 新增 C6/C6b 并改 live_probes 返回 (done, skipped) → 新增 `repo_config_check.py` 把 README 声称值与 ci.yml/电池实值等值对账 → 两判据入电池(28)与文档 → 报告 §10 / 05-08 记忆 / CHANGELOG / README 回写 → pathspec 提交 + push + SHA 核对
结构: OpenAPI 自定义键 `x-live-check{method,body,query,status[],required_keys[]}` 与 `x-live-skip{reason}`；覆盖恒等式 `探测 + 豁免 == spec 操作总数`（C6）且 `豁免 == 0`（C6b）；dependabot schema 键 `version/updates[].package-ecosystem|directory|schedule.interval|open-pull-requests-limit`，ecosystem 白名单取 GitHub 支持集
异常: 未标注操作既不计探测也不计豁免 ⇒ C6 直接判不等式失败（不再允许"静默漏"）；G4 在文档未同步时**必须报红**（本轮实证）；`--online` 网络不可达 → rc=2 而非降级为通过；memory 探测统一 `contract-probe` 且结尾 DELETE 自清；list 型响应的 required_keys 取首元素比对（空数组视为通过，避免"新库无数据"假红）
- **自审第三条（授权口径）**：整仓标 MIT，但 `src/vendor/gsap.min.js`/`ScrollTrigger.min.js` banner 自证为
  GreenSock Standard License（非 MIT/非 OSI，只有 three.js 是 MIT）⇒ 新增 `docs/THIRD-PARTY-NOTICES.md`
  逐文件声明 + README 中英改「MIT 仅覆盖自研代码」+ 判据 G6（vendor 每个 .js 必须在清单点名）/G7（README 必须引用清单）；
  `--selftest` 两类新反例（抹 gsap 行、抹 README 引用行）均被抓到 ⇒ repo_config 从 5 项变 6 项全 PASS。
- **本轮两处自我抓包留痕**：① 我新加的 `--slice` 开关第一稿是死代码（先滤 `--` 再判 `args[0]=="--slice"` 永不命中，
  `--slice 0 14` 实际跑了 28 条还报全绿）⇒ 改按 sys.argv 原样解析 + `--list` 打印过滤后条数 + 零套件直接 rc=1；
  ② `28/28` 一度在证据到手前写进报告（两次后台运行输出文件 0 字节）⇒ 撤回、前台复跑取数后才写回。
  同族第三次复发：shell 内联 python 传中文带引号文本两次 SyntaxError ⇒ 改正文一律用结构化编辑。

## r23 全量执行轮（QD 对标轮，同日第四轮，08:2x–08:5x）
- 数据侧连续第二轮**零漂移**（16/16 采集、0 处变化）⇒ 本轮把镜头从"守卫对不对"再前移一格：**查项目自己的强制登记项是不是空壳**。
- **抓到一条比上轮更严重的**：`memory/06-constraints.md` 的 R196「测试专用文件清单」至今是 init 模板占位符
  （`（如 eval/testset_provenance.json / blindset / frozen）`）⇒ 「新增评测数据先登记 provenance」红线**从未真正落地**；
  而 `_test/emotion-eval-dataset.json`（73 条 / 98.6% / 危机 6-6）是进《应用方案》与答辩口的数字，也是双端对账唯一数据源。
  最坏形态：把模型生成样本或真实对话灌进评测集 ⇒ 双端仍"逐项全等"（两边一起错），98.6% 退化成自证循环。
- 执行：① 06 填实为登记表（文件/条数/谁读它/provenance=人工撰写中文短句不用模型生成/冻结状态 + 「真实对话走 chat_message、
  与评测集物理分离、禁止回流刷分（PII 同源）」）；② 判据化 → `repo_config_check.py` **G8**（模板原句 `清单：（如 ` 残留 /
  登记文件不存在 R240 / 声称条数≠实际 items 数 / 未登记的裁决数据文件），`--selftest` 扩到**十类**全抓；
  ③ 顺带核实同节另一条声明：`git check-ignore -q` 对 `.env` / `.env.prod` / `.env.production` 三条全命中 ⇒ "已含规则"成立。
- **判据自己两次假红（先修判据、不动数据，R263 同族）**：
  ① 用"段里出现（如 "当占位符 ⇒ 填实后的表因说明行含中文括号示例被误判 ⇒ 改锚定模板原句；
  ② 对所有登记文件比 `len(items)` ⇒ 把 `vendor-manifest.json` 的"3 条"当 3 个样本（**分母混用**）⇒ 条数只对裁决用数据核。
  另修 selftest 样本⑨：把整篇正文当"节"传进判据，导致"删标题"看似没被抓到（实为样本构造错）。
- 取舍写明：`app.js` 469 行切分**本轮不做**（连续两轮登记的"下一件"）——同一轮里既改判据又动演示主链路文件，
  一旦电池报红无法归因；09-30 前剩 5 天，切分单独占一轮。仍在 07 P0。
- 回归：`repo_config --online` 7/7、`--selftest` 十类全抓；电池 `--slice 0 14` 14/14 + `--slice 14 28` 14/14 ⇒ **28/28 rc=0**。

【数据流假设】
来源: 实测 `grep -n "（如 " memory/06-constraints.md`（填实前命中模板行、填实后 0 命中）+ `python json.load('_test/emotion-eval-dataset.json')` 得 dict{meta,items} n=73、条目键 `text/expect` + `ls -l _test/*.json` 只有评测集与 vendor-manifest 两个数据件 + `git check-ignore -q .env / .env.prod / .env.production` 三条全命中（核实 06 既有声明）
流向: 06「评测集隔离」登记表成为**唯一人工维护源** → `repo_config_check.py` G8 读该节做四项对账（占位符/存在性/条数/漏登记）→ 判据入既有 `repo_config` 套件（电池条目数不变，README 的 28 无需改，G4 实测仍 PASS）→ 报告 §11 / 05 / 07 / CHANGELOG 回写 → pathspec 提交 + push + SHA 核对
结构: 抽条数用逐行正则（`([A-Za-z0-9_.-]+\.json)` + `(\d+)\s*条` 同行配对），不用跨行 DOTALL（第一版跨行匹配会把下一行的条数错配给上一个文件）；占位符判据锚定模板原句子串 `清单：（如 `；条数核对仅对 `eval_data_files()`（MANAGED_DATA ∪ 文件名含 eval/dataset/provenance/blindset/frozen）生效
异常: 章节缺失 → 直接返回"缺 R196 必填章节"；登记文件不存在 → R240 报错；JSON 不可解析 → 报错不静默；配置声明件（vendor-manifest）不按样本条数核，避免分母混用假红；判据自身变更必须配 `--selftest` 新反例（本轮加 4 类，合计十类）否则视为未验证

## r24 全量执行轮（QD 对标轮，同日第五轮，09:2x–09:5x）
- 外部有漂移 4 处（lobehub ★82,805→82,807 且 pushed 到 09-25、SillyTavern +1、OLV +1 但仍停更 4 个月）；七维无翻牌。
- **本轮主题不是抄别人，是修自己的排产**：`app.js` 切分在 r22、r23 连续两轮被我登记为「下一件」却没做 ⇒ 第三次登记 = 缺陷。
  本轮作为唯一改动开工：语音（TTS+ASR）外提 `src/js/voice.js`（99 行），`app.js` **471 → 403 行**，行为零改动。
  选它当第一刀的理由：与编排零耦合 + 判据最密（`ux_guards` U1 实测 utterance 构造计数 / `voice_check` A1-A6）。
- 交付链全跑通：`index.html` 顺序（voice.js 6813 < app.js 6849）→ `deploy_sync` PASS → `size_budget` 登记（4,148B/4,355B，
  关键路径 821,901 / 858,752）→ 公网重部署 `41dea397` → `live_sync`/`public_check`/`online_check` rc=0 → 电池
  `--slice 0 14` 14/14、`--slice 14 28` 14/14 ⇒ **28/28 rc=0**。`node --check` 双文件语法通过。
- **自伤两处（都留痕）**：① 同步命令把 `index.html` 误 cp 进 `deploy/xinyu/js/`，且用 `2>/dev/null` 吞掉报错 ⇒ `deploy_sync`
  报 EXTRA；上一轮我才把「禁配 2>/dev/null」写进报告，本轮自己就踩（同族 R212）。核实副本未被跟踪且字节相同后删除、复跑归零。
  ② 06 写「app.js 469 行」而实测 471 ⇒ 又一处文档数字脱节，已登记 G 系列未覆盖 06/05 正文数字这一缺口（是否扩面待拍板）。

【数据流假设】
来源: `src/js/app.js` 实测行号（TTS 277-316、ASR 318-348，用内容标记定位而非硬记行号；删段前 assert 首尾行内容）
      + `src/js/voice_check.py`/`ux_guards_check.py` 的判据面向（DOM 与 window 层，不依赖代码在哪个文件，先 grep 确认再动）
      + `git ls-files --error-unmatch` 与 `cmp` 实测（确认误造副本未被跟踪、与源字节相同，才允许删）
流向: 语音代码从 `app.js` IIFE 内部 → `src/js/voice.js` 的 `window.Voice`（init/speak/isSpeaking）→ `index.html` 在 app.js 前加载
      → `app.js` 调用点改 `window.Voice.init()` / `window.Voice.speak(r.reply)` → 同步 `deploy/xinyu/`（公网副本）
      → 判据：`ux_guards` U1 与 `voice_check` 打的是 jar@8123 上真实页面（行为面，不看文件位置）→ 公网重部署 → `live_sync` 逐字节复验
结构: 模块形态沿用 `window.X = (function(){...})()` 约定；localStorage 键与 DOM id 零改名；UTF-8 无 BOM；
      `size_budget.BUDGETS` 新增键 `src/js/voice.js`（实测 4,148B×1.05=4,355），总预算不变仍在限内
异常: `window.Voice` 缺席时 `init()` 内每个函数先判 `if (!btn) return;`（公网 stub 环境/脚本加载顺序错也不抛错）；
      浏览器不支持 `speechSynthesis`/`SpeechRecognition` 时仍走"隐藏按钮"旧分支（U1/A2 断言不变）；
      若 U1 或 A1-A6 任一报红 ⇒ 本刀回退（`git revert` 单提交）而非调参；`2>/dev/null` 教训改为同步命令一律不吞 stderr

## r25 全量执行轮（QD 对标轮，同日第六轮，10:1x–10:4x）
- 漂移 3 处（lobehub 82,808 / ST 33,745 / OLV 13,903），能力矩阵与 release/pushed 无变化 ⇒ 七维无翻牌；按上轮规矩继续推切分。
- **第二刀落地**：`src/js/chart.js` 外提（`window.Chart={render,init}`，3,780B），`app.js` **403 → 351 行**，行为零改动；
  约束随迁（HiDPI setTransform 逻辑绘制 / resize 200ms 防抖且无数据跳过）+ 补"缺画布静默返回"；`#btn-clear` 留编排（还管星图熄灭）。
- 交付链全跑：引入顺序 6813/6845/6885 → `DEPLOY-SYNC-PASS` → `size_budget` 17 文件（822,936/858,752）
  → 公网重部署 `0c90a2d9` → `live_sync`/`public_check`/`online_check` rc=0 → 电池 14/14 + 14/14 ⇒ **28/28 rc=0**。
- **同族坑第三次复发（本轮最有价值的自我发现）**：`size_budget` 补丁锚点写 `4_355`（实为 `4355`），`str.replace` 未命中
  却照常打印"已登记" ⇒ 被 coverage 判据抓出。与 r23 `done >= 8`、r24 `2>/dev/null` 同根：**把"没报错"当"生效"**。
  修法：补丁前后各加 assert；并登记做 `_test/patch_apply.py`（带 dry-run + 双向断言）让"改文件"本身有判据。
  另修一处脚本锚点过窄导致 `StopIteration`（改语义锚点 + 边界行 assert）。
- **台账缺陷顺手修**：07 的 P0 自驱条目被 r24 savepoint 迁进分卷后未回补 ⇒ 壳内只剩两条"等老大"，
  形式非空但无可自驱执行项；已补回第三、四刀 + 每刀固定动作序列。

【数据流假设】
来源: `src/js/app.js` 语义锚点定位（`// 7) 情绪曲线` 起、`#btn-clear` 前回溯到 drawChart 收尾 `}`、resize 注释起至首个 `});`）
      + 删除前 assert 段内含「图例」「addEventListener」，删后 assert `drawChart`/`getContext`/`measureText` 计数为 0
      + `_test/browser_check.py` 曲线断言（两态文案）作为行为证据；`curl` 实测 jar 吐 `chart.js` 200/3,780B 且 index.html 含引用
流向: 曲线实现体 → `src/js/chart.js` 的 `window.Chart` → `index.html` 在 app.js 前加载 → `app.js` 4 处调用改 `window.Chart.render()`
      + 一处 `window.Chart.init()`（resize 监听在模块内注册）→ `deploy/xinyu/js/` 同步 → `size_budget.BUDGETS` 新增键
      → 判据（电池 28）→ 公网 Pages 重部署 → `live_sync` 逐字节复验 → 05/06/07/ROADMAP/CHANGELOG/报告 回写 → pathspec 提交 + push
结构: `window.X = (function(){...})()` 约定与 r24 一致；localStorage 与 DOM id 零改名；UTF-8 无 BOM；
      预算键值用实测 3,780×1.05=3,969（先 grep 确认锚点为 `4355` 无下划线，再 assert 替换生效）
异常: 缺 `#mood-chart`/`#chart-count` → 静默返回（不抛错带走整页）；U1/U2/A1-A6/曲线两态任一报红 ⇒ `git revert` 单提交而非调参；
      同步命令一律不接 `2>/dev/null`（保留 stderr）；`str.replace` 类补丁必须双向 assert；jar 不重启亦须实测新文件可取（本轮实测 200）
- **同轮把"建议做的通用工具"真的做完了**：`_test/patch_apply.py`（锚点 0 命中即失败 / 歧义即拒 / 同义即拒 / 写后读回不一致即回滚 /
  临时目录备份 / `--selftest` 六类自证），入电池 28 → **29**。上线即抓到两件事：① README「28 套件」脱节被 G4 报红并由工具改正；
  ② 工具自身判据过严 —— 插入型补丁（repl 合法包含锚点）被误拒，dogfood 第一次就撞上，已修并补第 6 类样本。
  教训：**新判据必须当轮就拿真实动作喂一遍**，否则"过严"会以"我改不动文件"的形式拖到下一轮才暴露。
- 回归补记：`--slice 0 15` 15/15 + `--slice 15 29` 14/14 ⇒ 29/29；其后改工具自证样本与两处文档，复跑受影响套件
  （`--only patch_apply` 1/1、`repo_config` 6/6 含 G4 实测 29==声称 29）；未重跑全量，如实标注。

### r25 收尾三抓（13.6，写下来是因为都发生在"自己的账"上）
1. **记教训的表自己错标**：报告 §13.3 把「阈值掺水」记成 r23（实为 r21 写入、r22 抓出），并漏数 r20 的
   「BUILD FAILURE 但旧进程回 200 ⇒ 假健康」⇒ 同一族坑在三份文档里分别写着"三条"与"四轮"。
   处置：统一为四轮四形态（r20 假健康 / r22 阈值 / r24 吞报错+文档数字 / r25 静默空改），报告 + 07 + lessons.part67 三处同步（提交 71afa13）。
2. **记忆层自有红线没人量**：`split --check` 判 07 为「索引壳（主卷不拆）」却实测 5,795B > 4,096B（R161 零豁免）。
   上一轮只给 `src/` 建了 size_budget，memory/ 侧只有工具报警、无当轮动作。
   处置：主线目标背景块 + J3/J4 完整段**原文无损**迁 `07-next-steps.part17.md`，主壳留摘要+指针 ⇒ 3,533B，
   savepoint 复跑「✅ 07 卷 4KB 合规」，P0 四条可执行项不少（含两条待老大）。06 属"活载卷"，工具口径不要求主卷 ≤4KB，未动。
3. **savepoint 自愈迁移再次不提交**（05→part5/part6、07→part15/part16 留在工作区）：r24 已登记为待办，本轮又命中
   ⇒ 对这类问题"登记待办"无效，只能当轮补交（提交 b672376）。顺带把 sanitize 对 `_trash/` 的告警核实为**保留项**：
   `.gitignore:53` 已忽略该回收台账，移动它反而可能断掉还原线索 ⇒ 判"不改数据"，与 08 已登记的 review 误报同列。
- 收尾全量复跑（覆盖 13.6 全部编辑）：**BATTERY 29/29 rc=0 ALL-GREEN**。

## r26（同日第七轮）
- **数据分档**：外部 6 处漂移 ⇒ 实质 2（sapphire pushed 09-25 + release v2.5.0→v2.13.1）/ 抖动 4（★±1，含 lobehub 倒退）。
  新建 `classify_drift`（stars 归抖动），selftest 两侧都验（纯抖动场景必须 0 实质）。
- **切分第三刀**：`src/js/chat-window.js`（`window.ChatWindow={init,push,update,setTag,toBottom}`），`app.js` 351→**268 行**，
  行为零改动。判据是行为级 U2a–U2f（21 项）+ browser_check/stream_contract/voice；预算反向棘轮 app.js 23,979→14,777、
  新件 5,845（18 文件 824,777/858,752）；公网 `61ec115b`，线上 chat-window.js 200 且 5,566B 等字节；全量电池 **29/29 rc=0**（三次）。
- **第五次同族坑（骗到验证动作本身）**：`import benchmark_metrics` 做负控制 = 先联网采集再 `exit 0`，反例没执行却像通过。
  根因裸 `sys.exit(main())` 缺 `__main__` 守卫；全仓 3 个（含 `run_all_suites`，import 一次=29 套件重跑）⇒ 全补 + 新常驻判据 **G9**
  （十一类自证含两条反向样本），负控制重跑 BROKEN rc=1 / RESTORED rc=0。
- **并发写相互破坏**：11:28 对方覆盖我的 `lessons.part67`（我 r25 批丢），12:08 我同样覆盖对方 `part68`（2,515B）。
  已从对方 transcript 按字节复原 part68=2,515B；我方两批重建为 part70/71/72（均 ≤4096B）。
  规矩落进用户级记忆 `feedback-shared-memory-write-discipline.md`：先重列 max(partN)+assert not exists、只用 append/patch、写完复量。
- 记忆体积：05 自愈迁 3 条回 3,717B；07 人工迁 part18 回 4,040B；06 是"活载卷"不违规且**刻意不拆**（G8 直读其评测集章节）。
- 提交：e67ba30 / 1d35323 / dd8f09e（远端 SHA 已核对）。

## r27（同日第八轮）
- **判据先行**：先补 `_test/settings_panel_check.py`（S1–S8 + 五类注入反例），旧代码基线 9/9 绿 → 才外提
  `src/js/settings.js`（第四刀）→ 复跑同判据 9/9 绿。`app.js` 268→241 行（复算命令在 `memory/05`；
  第四刀提交信息里我手抄成 239，已留校正注）。预算再棘轮 app.js 14,777→13,780、settings.js 3,000。
- **selftest 记账 bug（同族第六形态）**：成功也 `append(None)` ⇒ 非空即判红，输出一条空 FAIL；改成只登记失败项后五类反例全抓。
- **全零补分母**：`coverage_hits` 把树截断/取数失败仓踢出分母并点名原因，恒等式 `usable+blind==总数` 不成立即 rc=1；
  实测核对 lobehub 树 `truncated=false` + 20,740 对象 + sw 类文件零命中 ⇒ `pwa_offline=0/16` 为真零。
  负控制（换回旧写法）实测 rc=1 —— 只有 r26 的 `__main__` 守卫之后才做得到，两轮基建互相兑现。
- 电池 29→**31**（README 声称数由 G4 对账 31==31）；公网 `4f8cd81b`，线上 settings.js 2,857B 等字节；全量 **31/31 rc=0**。
- 切分线收口（四刀完成）；下一件 = 只读离线壳 `sw.js`（对标反读：16 仓全零 = 差异化机会），
  硬门槛"先立一字节可见性时效判据 + `/api/**` 不落缓存断言，做不到就不上"，全文见 `memory/07-next-steps.part19.md`。
- 并发规矩兑现：写 `lessons.part76.md` 前重列卷号（最大已被推到 75，76 不存在才落盘）。
- 提交：33f5735 / b576b8d / b69905b / d3b74d5（远端 SHA 逐笔核对）。

## r28（同日第九轮）
- 全零反读成机会：`pwa_offline=0/16`（r27 分母证明过）⇒ 上线只读离线壳 `src/sw.js`。**判据先行**：
  先写 `_test/offline_shell_check.py`（A1–A10 + R1–R9 = 14 项、13 类注入反例）再写壳，R9 = 公网真断网重载，PASS 才部署 `54fb9778`。
- 判据教了四次：transferSize 无判别力→改 SW 自报 `x-xinyu-src`；`catch(()=>{})` 吞失败→留痕+两条分母断言；
  间歇红根因是**判据脚手架**（单线程 TCPServer）→A/B 翻变量钉因果；install 未完就数缓存→改轮询。
- 少写代码：Java `CacheHeaderFilter` 实测被 Spring 资源处理器的 no-store 覆盖 = 死代码 ⇒ 删除；Pages 侧只钉真正缺头两条。
- 🔴 失察并修复：CI 自 b69905b 起连续 4 个提交 failure（java-build 缺 PyYAML，本机装了看不见），我此前只报"本地电池全绿"。
  修：`_test/requirements.txt` 单一清单四 job 统一安装 + **G11**（清单==CI 真跑判据的实际 import，AST 扫，幽灵依赖也判红）；
  CI browser job 改整跑电池 `--exclude-llm`（豁免两条点名 + 恒等式）+ **G10** 盯覆盖分母。
- 三条反哺已闭环（走 A-skill-manager 链）：A-get-memory V4.31.0（附加一问 + 占号防护动作化）、
  consulting-analysis V1.6.0（M5 第⑦问分母证明）。复验：真相源 33 PASS / 账实 0/0/0 / 镜像 -Fix 后 mismatch=0；提交 bcefc8f。
- 提交：9f61edf / 6beed16 / 8d36323 / a669d20；本地 31/31（CI 同款命令）+ 33/33（含密钥）rc=0。

## r29–r30（同日第十、十一轮，合并记录）
- 受理面优先：`gh run` 三次全红且无日志 ⇒ 读 ANNOTATIONS 才看清真因是 **GitHub 账户账单/配额，runner 从未启动**。
  判据化 `_test/ci_status_check.py`（CODE_FAIL / ENV_BLOCKED / PASS 三态、HEAD-only、CI 内部 SKIP、两条反向断言）。
- 自己的 3.11 雷：聚合器里 `print(f"{" " * 25}· {d}")` 是 PEP 701 语法 ⇒ CI SyntaxError；已改拼接 + `compileall` 前置。
- 电池 33 → **35 套件**；G4 对账 35==35、G10 恒等式 实跑 32 + 豁免 3 == 35；`ci_status` 让电池**如实挂一条红**（未验不放宽）。
- 台账自证：self 行现已报 `caps=...,pwa_offline` ⇒ r28「离线壳是 16 家独有能力」这句话从此可复算，不再靠报告自说。
- 重录成片实测前置：pipeline 守卫要求同源代理链路（proxy 有值、base 空），本机是直连 ⇒ 一条切换 + sha256 备份还原后再录。
- r29 欠的文档账本轮补齐（CHANGELOG、报告 §17、07 阻塞登记）。提交 09d3096（r29 快照）等。

## r30（续）对标第十一轮 · 台账盲区点名 + 壳体积收口
- 受理面判定首跑即给 `ENV_BLOCKED`（HEAD sha=09d3096）⇒ r28 的 CI 覆盖面修复至今不可验，如实挂账不声称已验。
- 07 索引壳被 r30 写成 4612B（超 R161 的 4096B）⇒ 迁 `part20`（CI 三条判据 + 同族坑 r30 增补 + 成片重录三步前置），
  壳 4052B 达标（`handoff.py split --check` 亦报 4007B/上限内）。**两处自抓**：
  ① 拆卷脚本第一版「写 part20」在「体积断言」之前，失败即留半成品分卷 ⇒ 改为全部断言先于落盘；
  ② `read_text` 得 4562B 而磁盘 4612B，差 50 = CRLF 行数 ⇒ 体积红线一律按 `read_bytes()` 判。
- 台账新增 `blind_spot_caps()`：能力匹配器只看路径，对 `streaming`（SSE 在 5 个文件里）与 `e2e_browser`
  （20 个 playwright 套件在 `_test/`）是假阴性 ⇒ 单列「盲区点名」而**不计入 caps**（避免与参照仓双标）。
  自证三侧 + 三个变异体（恒空/恒报/不去重）实测均被 `SELFTEST-FAIL` 抓住。
- 快照刷新：漂移 3 处 = 实质 1（lobehub canary 号推进）+ 抖动 2 ⇒ 连续第七轮外部无可行动新信息。
- 电池 34/35，唯一红 = `ci_status` rc=2（设计如此，不放宽）。提交 42fe924（docs r30）+ 本轮后续代码提交。

## r30（再续）成片重录交付 + 一次真实失败的归因
- 把 07 登记已久的自驱 P0 做掉了：8 幕 `RECORD-PASS`、`ffprobe` 217.56s、成片 sha256=`fc810f65…`（替换旧 `50e060d1…`）。
  S3 画面标签实测 `在线大模型生成 · 逐字流式 · … · 情绪:后端 · 1378ms` ⇒ 后端引擎终于**在交付物里可见**。
- **第一次尝试 rc=1**（不是环境抖动，是我的 URL 形状写错）：代理写成 `http://127.0.0.1:8123/api/chat`，
  页面在 `localhost:8123` ⇒ 跨源，且 `/api/chat` 无 `Access-Control-Allow-Origin`（curl 带 Origin 实测），
  S3 落进「离线共情模板」被自身断言拦住。改**同源相对** `/api/chat` 即通过。
  结论：**"同源代理"的同源是 URL 形状的属性，不是"同一台机器"的属性**。
- 环境切换全程机器核验：`src/js/demo-config.js` 录制前后同为 `2521954c…`（try/finally + 字节比对），
  公网零密钥 stub 未触碰；录后 `public_check` / `live_sync` / `deploy_sync` 三项 PASS。
- 文档同步：07 P0 该项转 ✅（壳 3952B ≤4096，另把 GitHub 账单阻塞单列一条，不让它被划掉），
  `part20` 第三节改写为"已交付 + 失败归因"，`提交清单与验收状态.md` 第 2 行按磁盘实况更正
  （原写「视频本体 0 条，需真人录」是**过时状态**）并加一条 sha256 红线自查。

## r30（闭环）贴回的两条 footer 已落成技能文本 + r25 两条挂账一并处置
- A-get-memory **V4.32.0**：Step 2.5 第 8 条「失败面必须与成功面同条登记」（一手根因=本轮成片首跑 rc=1 的跨源归因）。
- consulting-analysis **V1.7.0**：M5 第⑧问「同一把尺对双方是否同构」（盲区单列、不并入 caps）。
- A-get-memory **V4.33.0**：Step 2.7「复发计数硬门槛」（补 r25 那条；同族坑第三次起禁写"下次注意"）。
- **一条改判**：r25 第二条（新判据须喂真实动作 + 红/绿两侧）实测**已由 V4.30.0 Step 2.7 三条硬判据承接** ⇒
  按 M5⑥「同一判断只有一处实现」不在 consulting 复制第二处。**旧挂账理由"agent 不擅改归属方资产"被本轮实测推翻**：
  治理链自走全绿（mirror-pass missing=0 mismatch=0 / verify 33 PASS / disk_registry_diff 0-0-0 / noise 零散射 /
  [source-guard] PASS / ls-remote HEAD 一致）。索引未重生已留痕（grep 证派生件不存这两个版本号 + 他人 12 个 WIP 在树里）。
- 全局提交：`D:/global_skills` 177f3e3 → 710ad37；镜像 `.agents/skills` c46f7d8 → f25deb0（四个短哈希 `git cat-file -t` 验真）。
  项目侧详情迁 `07-next-steps.part21.md`（4037B ≤4096），壳压到 3796B（顺手修一条**过时状态**：
  07 里"视频 ✅ 217s（建议重录）"已改为"已重录 217.6s"）。

## r31 开工（第十二轮）· 本次标识 offline-audit-r31
【数据流假设】
来源: 每条参照仓的两项 GitHub API 实测取数 —— `gh api repos/<full>`（description/homepage）与
  `gh api repos/<full>/readme --jq .content`（base64 解码，截 200KB）；本轮 16 仓已逐条实跑，
  全部 meta 可读、README 取到 479B~38,985B（零取数失败）。对照物 = 现有 `CAP_RULES["pwa_offline"]`
  （只读文件树里的 sw.js/service-worker.js/serviceworker.js/sw.ts 文件名）。
流向: 新纯函数 `offline_signal_class(text)` 把"离线字样"分成四类（app_shell / local_models_offline /
  ml_training_offline / none），只喂给台账的**第二条观测通道** `--offline-audit`；其结果写进快照
  `runs[-1].offline_audit`，并据此**修订报告与 CHANGELOG 里"0/16"那句话的措辞**；不改 `caps` 计数、
  不改覆盖率恒等式（`usable + blind == 总数` 由 coverage_hits 独立守着）。
结构: 快照新增键 `offline_audit = {repo: {"class": str, "evidence": str(<=120B)}, ...}`；
  分类函数输入是纯字符串、输出是四类之一的字面量（无副作用、无网络），因此可离线自证；
  台账打印行以 `离线双通道:` 开头，逐仓一行，末尾给"文件名法 N/16 vs 内容法 M/16（其中 app_shell=K）"。
异常: ① 单仓 API 取不到（限流/无 README/仓库改名）⇒ 该仓记 `unverified` 并**点名**，全零结论必须
  在 `unverified==0` 时才允许打印，否则打 `离线双通道: 分母不全（N 仓未取到）⇒ 不得据全零下差异结论`；
  ② README 里出现 "offline" 但语境是训练/构建（本轮 MASCOT 的 offline DPO 即此类）⇒ 判
  `ml_training_offline`，**不得**计入"对手有离线能力"；③ 本地模型离线（Open-LLM-VTuber）判
  `local_models_offline`，计为"有离线但形态不同"，措辞里必须与 app_shell 分开写；
  ④ 自证三向：合成样本四类各一条（含两条反向：训练语境不得判成 app_shell、纯前端 SW 语境必须判成
  app_shell）+ 变异体（把分类器改成"只要含 offline 就算 app_shell"，selftest 必须 rc=1）。

## r31 结果（第十二轮 · offline-audit-r31）
- 双通道实测：`app_shell=0/16`（与文件名法一致）、`local_models_offline=1`（Open-LLM-VTuber 桌面本地模型）、
  `ml_training_offline=1`（MASCOT 的 offline DPO＝误报源，已归因排除）、`unverified=0`。
  ⇒ 报告 §18.4 把措辞从"没人做离线"下调为"没人用浏览器 app-shell 做离线"。
- 判据自证：四类样本 + 两反向 + 零输入判 none；两变异体（恒 app_shell / 恒 none）rc=1、还原 rc=0。
  实现坑：`gh()` 会 json.loads 整个响应且非零即抛 ⇒ `--jq` 裸串必炸（首跑即死于此）。
- 全局侧：consulting-analysis **V1.8.0** M5⑨（全零须第二通道 + unverified≠0 禁打全零）+ 修掉手抄「六条问」。
  门禁全绿：mirror-pass / verify 33 PASS / disk_registry_diff 0-0-0 / [source-guard] PASS。
- 自抓两条：① 开工口头说"公网离线从未实测"是错的（R9a/R9b 就是线上断网断言，实测 PASS 后当场改口）；
  ② 用既有行前缀当 Edit 锚点，把 06 里 r30 那条红线的表头吞了半行 —— 靠 `git diff --stat` 只见 +7 不见 -M 发现并修回，
  已作为第五排障条目写进 `memory/AGENTS.md`。
- 提交：项目 `6ea85e0`（已 push，ls-remote 一致）｜全局 `6838020` + 镜像 `cccf8a2`｜经验 lessons.part92（占号 O_EXCL）。

## r31 续（贴回的未闭环项已落地）
- **A-memory-start V10.72.0**：`dag_precheck.py` 报因升级 —— `require_mark` 找不到时把「块体起止判据 +
  只扫两处当日日志根」原文写进 reason（新增 `MARK_HINT`/`require_mark_repr`）；自测 7→8 条，
  新增回归样本「标识只写在标题行」，变异体（hint 退化旧文案）实测该用例翻红 `rc=1`、还原 8/8。
- **一次被自己门禁量到的红**：首版注入面摘要 389B 顶爆 C25 棘轮（63510>63336），
  未抬基线、未改他人条目，把自己那行压到 178B → `[GATE:stub-pass]`；中途一次压缩只省 59B
  被脚本内 `saved >= 70` 断言拦下没落盘（瘦身也要带判据）。
- 治理链：verify 33 PASS（C13 验版本线三步同步）/ mirror missing=0 mismatch=0 / disk_registry_diff 0-0-0 /
  stub-pass / noise 零散射。提交 全局 `eb53991` + 镜像 `a29d2ef`；`ls-remote` == HEAD。
- 项目排障手册第 1 行随之更新（该判据现已由工具自己说，不必再读源码）；经验落 lessons.part93。
- 本轮起 07/06/报告无改动需求（上一提交已含 r31 主线成果）。

## r32 交付物回扫（第十三轮）· 标识 deliverable-rescan-r32 / offline-badge-honesty-r32 / greeting-tag-honesty-r32
- 三条 G8 假设块各自过 `[GATE:dag-pass]`（新报因文案第一次实战就帮我省了一轮：标识写在块体内，一次通过）。
- 方案 HTML 补 6.7 离线壳 / 7.8 设置面板 + 实拍图 8/9、7.3 补 SSE、6.6 耗时改区间；PDF 重渲 **20 页 9 图**（贴上限）。
- 新常驻判据 `_test/pdf_leak_scan.py`（电池 35→37）：两轮字体噪声误报都先改判据再下结论；
  并诚实记下"中文在 Edge PDF 里是字形 ID ⇒ 校名扫描交给本判据＝假覆盖"，该模式已删，校名仍由目检负责。
- 修两处"伪装在线"出口（徽章看 navigator.onLine、开场白标签），判据 R10a/b/c + 变异体双侧实测 + 06 新红线。
- 自抓三条：① heredoc 吞反斜杠（我自己写进排障手册那条，本轮又踩，改判为"必须落脚本文件"）；
  ② 一次"看似无改动"的 Edit 把两行粘连成语法错误；③ CHANGELOG 插入时留下空的 `### Known issue` 重复标题。
- 待办：公网副本落后本地（07 已登记为下一件，未部署就不声称一致）。

## r32 闭环（贴回两条挂账已落技能正文）
- consulting-analysis **V1.10.0** 加 M6「材料是否追上被描述对象」（交付一致性回扫）+ 禁令改双向；
  A-get-memory **V4.34.0** Step 2.7 加第④条「扫描类判据先证看得见目标内容」。
  门禁全绿（mirror/verify 33/registry/noise/[source-guard]），提交 32d5071 + 镜像 eb3b9e3；`[GATE:evolution-pass]`。
- 本轮不新开 lessons 卷并留理由：两条经验的持久形态就是刚落进技能正文的那两条规则（含一手实证与复算命令），
  再抄一份进 lessons 即造两处真相。

## r33 公网重部署（第十四轮）
- deployment `b5f46ecf`（回滚锚 `ec881aaa`）。部署前避掉一个会让线上挂的坑：`deploy/xinyu/` 内没有
  `functions/`，Functions 源在 `deploy/functions/`，wrangler 按 **cwd** 解析 ⇒ 必须 `cd deploy` 再部署，
  且日志须见 `Uploading Functions bundle`，否则 `/api/chat` 掉 404。
- 四项复验全绿：`LIVE-SYNC-PASS`（9,463B 逐字节等）、`PUBLIC-ONLINE-ALL-PASS`（标签「在线大模型生成 ·
  逐字流式 · 词典+LLM 分歧 → 采信 LLM · 2184ms」/ 危机 True / console 0 / KEY_LEAK False）、
  `/api/chat` 200、公网 `app.js` 含「网络不可用」×3 + `chat-window.js` 含「本机开场白」×1。
- 漂移 **0 处**（首次连 ★ 未动；但两次采集只差 22 分钟，不读成"对手停止演进"）。
- 自抓两条：① 第一次算漂移把 r31→r32 的差当成 r33（未核"上一次"是哪一轮）；
  ② CHANGELOG 编辑时把 r32 段引用的左括号吃掉（`r322026-09-25`），靠 `git diff --stat` 纯 +24 无删除才确认修全 ——
  与 r32 那条"锚点吞表头"同族，第二次踩在同一类动作上。
- 下一件（07 已登记）：成片录在 r32 徽章/开场白修复之前，画面仍是旧标签 ⇒ 按 M6 重录。
