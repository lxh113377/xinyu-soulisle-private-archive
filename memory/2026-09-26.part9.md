# 2026-09-26 会话日志 · 卷9（r38 续卷：按 4KB 同尺自卷8 续拆，条目逐字未改）

## 三、本轮最值钱的一课：新判据第一次真验它的地方不该是 CI

首版把护栏判定写在 `proxy.hasKey()` **之后** ⇒ runner 没密钥，请求走不到护栏，
`safety_guard rc=2` 把浏览器回归 job 判红（run `36239668456`）。两条路：塞进豁免名单
（＝CI 从此永不验护栏）或让护栏在无密钥下也可观测。选后者：判定前移、四条出口统一带头；
判据按有无密钥切状态码期望值，契约子项无密钥时记 **skipped 而不是 PASS**。

**手工按 CI 条件复现**（另起剔除 DEEPSEEK_KEY 的 :8124）又抓到第二层问题：头断言全过、
期望值还写着 200 ⇒ 6 条假红。修后门铃两侧实测：无密钥 15 验 + 6 skipped rc=0；有密钥 21 验 rc=0；
CI 现测 `BATTERY: 44/44`（实跑 44 + 豁免 3 == 47，恒等式自印），run `36240409204` success。

## 四、构建守卫（同一坑两轮三次的结构性解法）

r37：repackage 改名失败把 jar 截成 48,907B；本轮：clean 删不掉被进程锁住的 jar（rc=1）。
"记得先停服务"不可靠 ⇒ `_test/build_jar.py`：停 → 构建 → **验货**（体积下限 / 内嵌 version==pom /
密钥 0 命中 / jar 内前端件 0 条）→ 可选重启；**验货不过就不产资产**。
顺手记一条跨执行器坑：python 里 exec sh 版 `mvn` 报 `WinError 193`，必须用 `mvn.cmd`。

## 五、版本与产物

`v1.4.1 → v1.4.2 → v1.4.3` 三个 Release 远端实测 `draft=false`、各 2 资产
（最新 jar 28,445,186B 内嵌 version=1.4.3；web 包 249,731B 与 1.4.2 内容一致）。
本机全量：`--slice 0 24` ⇒ 24/24、`--slice 24 47` ⇒ 23/23 ⇒ **47/47 rc=0 ALL-GREEN**。
