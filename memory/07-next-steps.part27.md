# 07 - 下一步 · 卷27（r38 · 2026-09-26 晚：安全策略第二层 + 判据接入 CI 的前置复现）

## 一、r38 已完成（全部有远端回执）

1. ✅ **输入侧护栏 `SafetyGuard.java`**（8 类模式：指令覆盖中英、系统提示词套取含「把…输出」倒装、
   角色伪造、persona 越狱、编码载荷；单轮 >4000 字截断；命中即向上行 messages 追加系统重申）。
   **边界**：响应体一字不改（AC-OBS-08 逐字透传；流式改写要整段缓冲＝废掉流式），
   判定只落响应头 `X-Xinyu-Safety`；输出侧只做分类，最高危自伤由危机层兜底。
2. ✅ **行为判据 `safety_guard_check.py`**（电池 45→**47**）：HTTP 双向 —— 注入 6 例必须点对应类别、
   正常 6 例（含 2 例近似误伤）必须判 0、危机句不误伤、致死句 `risk=high`、超长 `capped=1`、
   流式同带头；`--selftest` 四向。判据接上当轮即抓到**自己的第一条真漏报**（中文倒装语序）。
3. ✅ **CI 红归因并修**：首版判定在 `proxy.hasKey()` 之后 ⇒ runner 无密钥走不到护栏，
   `safety_guard rc=2` 判红浏览器回归 job（run `36239668456`）。改判：判定前移 + 判据按
   "有无密钥"切状态码期望值 + 契约子项无密钥时记 **skipped 不记 PASS**。
   现测 CI `BATTERY: 44/44`（实跑 44 + 豁免 3 == 47 恒等式自印），run `36240409204` success。
4. ✅ **`build_jar.py` 构建守卫**：同一坑两轮三次（r37 repackage 截断 jar 成 48,907B、
   本轮 clean 删不掉被锁 jar）⇒ 停(8123/8124 持有者)→构建→**验货**（体积下限/内嵌 version==pom/
   密钥 0 命中/jar 内前端件 0 条）→可选重启；**验货不过不产资产**。
5. ✅ **版本链 v1.4.3** 已发 Release（jar 28,445,186B + web 249,731B，远端 `draft=false`）；
   v1.4.1/1.4.2/1.4.3 各带 2 资产 ⇒ 发布节奏第一次有可核对的回执。

## 二、新登记待办

- [ ] **P2｜对抗评测集**：注入/越狱样本目前只活在判据夹具里。要进评测面须**另立新集**
      （禁动 73 条冻结集，R196 评测集隔离红线）+ 先在 06 登记表写 provenance。触发条件：复赛后再做。
- [ ] **P2｜输出侧改写**：需与"逐字流式"契约一起重新设计（缓冲 or 分段丢弃），
      当前明确不做，理由已写进代码注释与 README「安全策略」。
- [ ] **P3｜runner 条件预演自动化**：本轮靠手工起 :8124 剔除密钥复现。可固化为
      `--no-upstream` 模式或 CI 第二个 job，避免下一个人又靠记忆。
