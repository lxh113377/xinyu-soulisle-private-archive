# 07 - 下一步 · 卷31（r40 · 2026-09-26 夜：性能维 + A-skill-manager 入场三闸）

## 一、r40 已完成

1. ✅ **性能维从「未实测」变成常驻判据**：`_test/perf_baseline_check.py`（电池 47→**49**，恒等式
   「实跑 46 + 豁免 3 == 49」由脚本自印）。口径：本地无外网四目标 + 8×6 并发；
   **危机短路路径必须先证 `llm=null`**（守卫优先于性能断言，取不到算未验不判绿）。
   三轮实测：静态/vendor/health/危机情绪 p95 **15.5–27.3ms**；并发 **2202–2484 rps**、p95 3.6–4.3ms。
   阈值 = 实测 ~16–25 倍余量（p95 400ms、吞吐地板 50 rps）⇒ 只防塌方级退化；**先量变异再接线**。
2. ✅ **横向可比性先取证再声明**：16 仓两通道探测性能工程件——原始计数含假阳性
   （`bench`⊂`workbench` 28 条、`Performance` 命中 NuGet 的 `System.Diagnostics.Performance*.dll` 7 条），
   语境归因后**仅 sapphire `tools/tts_benchmark.py` 1 家明确**，且无一家公开可比延迟/吞吐数值
   ⇒ 报告与红线都改成「本维不可横向比较，只做自身棘轮」（06 新增「性能口径红线」）。
3. ✅ **A-skill-manager 入场三闸实测**（该 skill 本轮才真正加载）：`G-a` 脏度 22 项、
   `G-b` 焚诀 **37 个已暂存未提交** + 62 在途、`G-c` HEAD 5470b01/16:55
   ⇒ 按铁律 10 **禁 rebuild、禁产基线**，本轮只走只读通道
   （`build_registry --dry-run` 注册表零漂移；镜像 `[GATE:mirror-pass]`）。

## 二、本轮两处自我更正（都是守卫把我拦住的）

- **样本错而非代码错**：我造的危机句「不想伤害自己」是否定式、没命中词表 ⇒ 打到 LLM，
  量出 p95 772–905ms，我还差点据此上报「服务端危机没短路」这个不存在的缺陷。
  读 `EmotionClassifier`（确有 `词典·危机拦截`）+ 换用**冻结评测集**真危机句后 `llm_used=False`、16–25ms。
  ⇒ 规则化：**性能/评测类样本一律取自已登记数据集，禁止现场造句。**
- **索引壳只留未收口项**：05 壳经 trim-shell 后 ✅ 全部迁分卷，我原本要往壳里加「性能基线 ✅」一行
  是**逆着壳语义**的 ⇒ 改为写进 `05…part12.md` + 壳里只加分卷指针。

## 三、新登记待办

- [ ] **P2｜危机短路在两侧都写成显式 AC**：`emotion_wiring_check` 断言的是**前端**不经后端，
      本次实测的是**服务端** `/api/emotion` 短路 —— 两个命题目前各自有守卫但**没有一条 AC 同时点名两侧**，
      补法是在 08-AC 加一条并让 `engine_consistency` 附带断言 Java 侧 `path=词典·危机拦截`。
- [ ] **P2｜性能基线接入 CI 后的首轮观察**：CI runner 与本机档位不同（Linux/无桌面），
      首次上线若因环境差异判红，按「阈值来自本机」这一口径处理（重定基线而不是抬 timeout 掩盖）。
- [ ] **P3｜真机 FPS / Lighthouse**：headless 数字不可信，需真机；沿用 05 的 ❌ 未实测标记。
