# 2026-09-26 会话日志 · 卷6（r37 · 傍晚轮：对标换面 → 交付可得性 → 远端可见面）

> 同一条对标指令第三次重发 ⇒ 按"重发=完整重执行"跑 r37；台账复采与 r36 **零实质漂移**
> （只 2 处 ★ 抖动）⇒ 老问题问完了，本轮换观测面。

## 一、换面之后才看见的三条差距

1. **交付形态**：官方口径是「链接稳定可访问 **或 源码**」，我六轮都在优化"链接"，从没发过 release。
   同池 16 仓实测：**近 90 天有发布 7/16**、从未发过的只有 5 个（且都比我有声量）。
2. **能力位≠行为**：`deps_autoupdate` 是我 1/16 才有的稀罕配置（peers 只有 opensoul 配了），
   但它产出的 4 条 dependabot PR **挂了 2 天没人处理** ⇒ 账面指标看不出这种空转。
3. **远端可见面无守卫**：仓库名带 `private-archive` 而 visibility 实测 **PUBLIC**；
   既有 `tracked_secret_scan` 扫的是本机 `git ls-files`，而 `.gitignore` **不会把已推上去的东西拿下来**
   ⇒ "本机干净"与"评委看到的树干净"中间没有判据。

## 二、处置（都有远端回执）

- ✅ **首个 GitHub Release `v1.4.1`**：fat jar 28,438,588B（内嵌 `version=1.4.1`、0 个前端文件、
  0 处密钥形态、`mybatis-plus-core-3.5.17` 在册）+ 零密钥前端包 249,409B/23 文件（逐文件扫 0 命中）。
  远端实测 `isDraft=false`、2 资产。版本三源同步：`pom == 文档 == tag`；
  G12 在 tag 未打时**如实判红「发版链断在中间」**⇒ 用 `git push origin main refs/tags/v1.4.1`
  单事务推两 ref，避开"CI 先跑、tag 后到"的竞态（run `36236951758` success）。
- ✅ **dependabot 3/4 合并**（#4 mybatis 3.5.17 / #1 checkout 7 / #3 setup-python 7），
  三次 push 的 CI 逐项 success。#3 与 #1 同改 `ci.yml` 冲突 ⇒ `@dependabot rebase` 让工具自解，
  **不在他人分支上手改**。#2 Spring Boot `3.2.5→4.1.1` **判为截止前不做**并写清重评触发条件。
- ✅ **新判据 `remote_tree_audit.py`**（电池 43→**45**，README 声称值同步，G4/G10 复算 PASS）：
  扫 origin 默认分支树，实测远端 230 blob 零命中；4 条形态命中逐条复核均良性
  （`.env.example` / 扫描器自身 / 零密钥 `demo-config.js` / 读配置的 `ApiTokenFilter.java`）。
