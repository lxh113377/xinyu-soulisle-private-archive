# 2026-09-26 会话日志

## r35 全量执行轮（QD 对标轮，11:4x–12:5x）· 本次标识 ci-green-r35

【数据流假设】
本次标识: ci-green-r35
来源: `gh run view 36136685234 --log-failed` 原文（HEAD=ebf0736）+ 本机 `python _test/ci_status_check.py`
      + `_test/{settings_panel_check,voice_check,pdf_leak_scan,run_all_suites,repo_config_check}.py` 现读
流向: 判据/CI/文档改动 → push origin/main → GitHub Actions 两条 job 复跑（受理面是唯一收口判据）
      → 台账 `交付物/对标数据/benchmark-metrics.json` → 报告 `交付物/对标分析报告-2026-09-26.md`
结构: 判据脚本 rc 约定 0=PASS / 1=判红 / 2=环境未验，其余 rc 一律按 CRASH 点名；
      `git ls-files` 扫描输出 `路径:行号`；文档迁移以「条目」为最小单位，先闭合断言再落盘
异常: 远端仍红则读现场行归因，禁放宽阈值凑绿；迁移脚本断言失败即中止（本轮实测拦下两次圈错范围）；
      `src/js/demo-config.js` A/B 前后 sha256 同为 `2521954c4f2087e330d0f3880dbf60eb0db03789f05a360bed14a670980be6d0`

### 开局就推翻了两条在册口径（这是本轮的入口）

1. `memory/07` P0 写着「⛔ 只有老大能解：GitHub Actions 账单/配额 ENV_BLOCKED」——
   实测 HEAD 最近 run 已判 **CODE_FAIL**，且**最近 5 次 main push 全红**（`gh run list` 逐条 conclusion=failure）。
   计费早就恢复了，红的原因换了，而电池收口行两次的样子一模一样（`RED: ci_status`）。
2. `README.md` 写着「四条 job 均在 GitHub 真跑验证」+「全量电池 37 套件 rc 全 0」——
   两条都是状态性表述未随受理面变色回扫（R242 的反面案例，本轮抓到的是我自己上一轮的写法）。

### 三条 CI 真红的逐条处置

| # | 失败面 | 归因状态 | 处置 |
|---|---|---|---|
| 1 | 同步守卫 job 的密钥步：`_test/pdf_leak_scan.py` 命中 | ✅定案 | 反例样本改拼接；新建 `_test/tracked_secret_scan.py` 把判定收进一处，`ci.yml` 改调它，并**进电池** ⇒ CI 与本地同一把尺（原先同一判断两处实现，只有 CI 会红） |
| 2 | `voice` A4 未观察到监听态 | ✅定案（CI 事件序列 `['start','error:audio-capture','end']`）| 抽纯函数 `no_input_device(ev)` 供 A4/A7 共用；**顺手收紧**：`not-allowed` 不再算环境借口；新增常驻套件 `voice_selftest`（5 例含两个反向方向）|
| 3 | `settings_panel` S3/S4/S5/S7b | ❌**未定案，不声称已修** | 已排除 `demo-config` 变量（换 CI 版 stub 本机复跑 9/9 绿）；给判据装完成态等待 + 报红自动附现场 `{open,returnValue,saveDone,domBase,cfg,ua}`；补两类反例（⑥ 竞态实测、⑦ 保存不关窗必超时）。下次 run 读现场行定案 |

### 本轮自抓的三处（都写下来，因为它们都是"我以为我对了"）

1. **我的新分类器少一挡，把崩溃洗成"环境未验"**：收口行第一版写「非 1 即环境」，随后 `voice` 两条套件
   硬崩（`rc=0xC0000409`、stdout 全空）就被印成 `ENV-UNVERIFIED`。现分三类，并用合成四态套件双向验
   （PASS 不进红名单 / 崩溃不冒充环境 / 有红必 rc=1）。
2. **迁移脚本的 4KB 断言量错对象**：`assert len(new_shell.encode("utf-8")) <= 4096` 过了，磁盘却 4,128B ——
   `pathlib.write_text()` 在 Windows 把 `\n` 译成 `\r\n`，每行少算 1B。改为按**写后回读字节**判。
   同族第一形态（`ls -l` 口径 vs 字符串口径）已进 `memory/AGENTS.md` 排障手册。
3. **G13 上线当轮抓到自己两处**：`check("G6+G7 …")` 合并项被首版正则漏取 G7（判据过敏，
   按 R263 先修判据不动登记）；以及 G11 落地时从未写进头部清单（漏登记）—— 于是有了 G13 自己。

### 台账与判据

- `benchmark_metrics.py`：16/16 仓，漂移 11 处 = 实质 3（lobehub pushed 09-26 / canary.22 / **CI wf 49→50**）+ 抖动 8。
- 新增判据：`tracked_secret`（+ 其 selftest）、`voice_selftest` ⇒ 电池 37 → **40**；README 声称数由 G4 机器对账。
- `repo_config` 新增 **G12**（版本三源对账：tag == pom == 文档「当前版本」）与 **G13**（判据账本自洽）。
  实测抓到 `ROADMAP.md` 版本行停在 v1.3.0（tag/pom 均 1.4.0）⇒ 改文档后转绿。
- `ROADMAP.md`：给「PWA/SW 撤销不做」补 **更正注**（该条已被 r28 的只读离线壳推翻，却与同文件第 94 行并存，
  违反该文件自己第 156 行的纪律）；`ci.yml` 电池步骤名去掉手抄的"30 条实跑 + 3 条豁免"。
- `memory/05-feature-status.md` 追平 r29–r35（07 登记的 P0 自驱项）：主壳加「当前构建状态」权威段，
  r20–r28 各轮残段逐字迁 `part10.md`；主壳 3,798B ≤4,096B，**逐行复核原始行丢失 0**。
- `memory/07-next-steps.md`：4,999B 超限 ⇒ 按「条目」为最小单位迁两条已完成项到 `part23.md`，主壳 4,012B。
  （第一版用行区间锚点，把中间新插入的未完成项圈了进去，断言当场拦下、未落盘。）

### 一处安全事实（只登记，不复制内容）

- 工作区里除 `src/js/demo-config.js` 外，`_test/cors_probe.py` 也硬编码了一个真实形态的 Key（前缀 `sk-900f…`）。
  两条都在 `.gitignore` 内、HEAD 干净（`git grep` 跟踪件 0 命中）。**撤销/轮换归老大**；
  本轮把「跟踪文件密钥形态」做成了常驻判据，今后若有人把它 `git add` 进去会当场判红。

### 下一件（07 已登记）

1. 读 push 后的 CI 现场行，给 `settings_panel` 那四条定案归因（判据已具备自证面）。
2. Playwright 起假媒体设备，把 `voice` 的 SKIP 换成真断言。
