# 2026-09-26 会话日志

## r35 全量执行轮（QD 对标轮，11:4x–12:5x）· 本次标识 ci-green-r35

【数据流假设】
本次标识: ci-green-r35
来源: `gh run view 36136685234 --log-failed` 原文（HEAD=ebf0736）+ 本机 `python _test/ci_status_check.py`
      + `_test/{settings_panel_check,voice_check,pdf_leak_scan,run_all_suites,repo_config_check}.py` 现读
流向: 判据/CI/文档改动 → push origin/main → GitHub Actions 两条 job 复跑（受理面是唯一收口判据）
      → 台账 `交付物/对标数据/benchmark-metrics.json` → 报告 `交付物/对标分析报告-2026-09-26.md`
结构: 判据脚本 rc 约定 0=PASS / 1=判红 / 2=环境未验，其余 rc 一律按 CRASH 点名；
      `git ls-files` 扫描输出 `路径:行号`；文档迁移以「条目」为最小单位，先闭合断言再落盘
异常: 远端仍红则读现场行归因，禁放宽阈值凑绿；迁移脚本断言失败即中止（本轮实测拦下两次圈错范围）；
      `src/js/demo-config.js` A/B 前后 sha256 同为 `2521954c4f2087e330d0f3880dbf60eb0db03789f05a360bed14a670980be6d0`

### 开局就推翻了两条在册口径（这是本轮的入口）

1. `memory/07` P0 写着「⛔ 只有老大能解：GitHub Actions 账单/配额 ENV_BLOCKED」——
   实测 HEAD 最近 run 已判 **CODE_FAIL**，且**最近 5 次 main push 全红**（`gh run list` 逐条 conclusion=failure）。
   计费早就恢复了，红的原因换了，而电池收口行两次的样子一模一样（`RED: ci_status`）。
2. `README.md` 写着「四条 job 均在 GitHub 真跑验证」+「全量电池 37 套件 rc 全 0」——
   两条都是状态性表述未随受理面变色回扫（R242 的反面案例，本轮抓到的是我自己上一轮的写法）。

> 中段（三条真红处置表 / 自抓三处 / 台账与判据 / 安全事实 / 下一件）全文见 `2026-09-26.part1.md`（r35 换卷，逐字未改）。

## r35 收口（同日，13:2x）· 标识 ci-green-r35

- **受理面首次全绿**：run `36220200506`（sha `7aac7d2`）四条 job 逐项 `success`；
  `ci_status_check.py` 由 `CODE_FAIL` 转 `CI-STATUS-PASS`（rc=0）。
  上一轮在册的"只有老大能解（账单/配额）"至此闭环 —— 真因从来不是配额，是三条代码级缺陷。
- 三轮 push 的完整链条（每一步都有远端证据，不是本机推断）：
  1. `2f5f638` → run 36219657886：37/38，**唯一红是我本轮新加的 G12**（CI 浅克隆无本地 tag ⇒ "读空气"）；
  2. `7aac7d2` → run 36220200506：**四条 job 全 success**；
  3. 文档收口 `f4f0df8` / `dd1ab76`。
- 本机复跑两段（同一份代码状态）：`--slice 0 21` ⇒ 21/21、`--slice 21 41` ⇒ 20/20 ⇒ **41 条全绿**。
- `settings_panel` 四条的最终口径：**修法有效已被受理面证实，机制解释仍不完整**
  （本机测不到"点完就读"的差异，CI 侧改成等完成态就绿）⇒ 只记"有效"不记"已解释"，
  `diag` 现场取证与两类反例长期保留。
- 本轮新增/收紧的判据共 6 处：`tracked_secret`（±selftest）、`voice_selftest`、`preflight`、
  G12（版本三源 + tag 面 ls-remote 回退）、G13（判据账本自洽）、电池收口三分类（RED/ENV/CRASH）。

### 本轮我自己踩到的四个坑（一手，已进 `memory/AGENTS.md` 排障手册或 CHANGELOG）
1. 收口行"非 1 即环境" ⇒ 判据硬崩（0xC0000409、stdout 空）被洗成"环境未验"；补第三类 CRASH + 合成四态双向验。
2. `pathlib.write_text()` 在 Windows 把 `
` 译成 `
` ⇒ 4KB 断言比字符串长度就少算每行 1B（断言过、磁盘 4128B 超限）。
3. `python x.py | tail; echo $?` 取的是 `tail` 的退出码 ⇒ 崩溃的判据被我读成 rc=0；改"重定向到文件后取 `$?`"。
4. 新判据带环境假设（只读本地 tag）⇒ 本机恒真、CI 恒红；被 CI 拦下后补 `ls-remote` 权威面 + 四条反例。
