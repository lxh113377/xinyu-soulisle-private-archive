# 07 - 下一步 · 卷28（r39 · A-project-better Step 0 遗留台账 + 逐条实测处置）

> 取证时刻 2026-09-26 20:23–20:31；采集源＝07 壳与分卷 / 根 TODO·ROADMAP / 05 / task-card /
> 会话任务清单 / junction 双跟踪 / 对话上下文（Step 0 §0.1 全 7 源）。
> 门禁偏差如实记：Step 0 要求按钮确认后台账才作数，本轮按用户令「严禁询问、自动化任务」
> 与「选中回传=授权」直接续跑，**台账照常编号并在此留证**，不假装拿到了确认。

## 一、台账（# | 来源 | 原文压缩 | 实测 | 结论）

| # | 来源 | 待办 | 实测取证 | 结论 |
|---|---|---|---|---|
| 1 | 07:14 | iCAN 报名 PII，09-30 截止 | 只能老大填（五人学号/手机/邮箱＋教师） | **仍有效 P0**（agent 不可执行） |
| 2 | 07:19 | J3/J4 变现③ fat jar 部署到可达机器 | `gh pr list`/目标机信息均无 | **仍有效 P0**（阻塞在用户） |
| 3 | 07:35 | 同族坑收口（台账 part18/20，G9 常驻） | `repo_config --selftest` 在册 | 仍有效 P2（长期项） |
| 4 | part26:25 | Spring Boot 3.2.5→4.1.1（PR #2） | `gh pr list` 现测队列**只剩它** | 仍有效 P1（截止前不做，理由在册） |
| 5 | part26:29 | 依赖队列 SLA（每轮跑一条命令） | 本轮首跑该命令：OPEN 仅 #2 | **已满足**（接续为节奏项） |
| 6 | part26:31 | Release 资产自动化 + 资产面密钥门禁 | 现仍手工 `gh release create` | 仍有效 P2 |
| 7 | part26:34 | Dockerfile 本机从未构建过（无 Docker） | 本轮把 build + **容器起服三项自证**接进 CI | **本轮执行**（待 CI 回执） |
| 8 | part27:24 | 对抗评测集（注入样本进评测面） | 73 条冻结集未动 | 仍有效 P2（须另立集＋登记 provenance） |
| 9 | part27:26 | 输出侧改写 | 与逐字流式契约冲突 | 仍有效 P2（赛后与契约同批重设计） |
| 10 | part27:28 | runner 条件预演自动化 | 本轮仍手工起 :8124 复现 | 仍有效 P3 |
| 11 | 05:9 | 仅剩 fat jar 部署 | 同 #2 | 与 #2 同义（合并，不重复出建议） |
| 12 | 05:15 | 「仍存差距」含 `/api/emotion` 接线·PWA·`app.js` 拆分 | `grep src/js` 命中后端接线；`sw.js`+`manifest` 存在；`src/js` 12 模块 | **三条已完成却挂在差距里 = 文档落后于构建** → 本轮修正（迁 part12 留痕） |
| 13 | 05:39 | three.js r128 vs 上游 | `vendor_freshness --check-upstream` 实测 STALE r128→r186 | 仍有效（头号技术债，截止后独立阶段） |
| 14 | 根 TODO/TASKS/ROADMAP | 未完成项 | `ls TODO.md TASKS.md` 无文件；ROADMAP 无 `- [ ]` | 未建档（无欠项） |
| 15 | junction 双跟踪 | memory/ 是否两仓各跟踪一份 | 项目侧 `git ls-files memory`=61；memory/ 是真实目录非 junction；全局无本项目副本 | **无双份跟踪**（R272 项排除） |
| 16 | task-card | 上轮「待确认未执行」区 | `grep 待确认｜未执行｜挂起` 0 命中 | 无挂起项 |
| 17 | 会话任务清单 | 未收口项 | TaskList 9 条全 completed | 无欠项 |
| 18 | 全局侧 | noise 门禁 heartbeat 豁免不生效（须 untrack） | r36 已归因，属主侧动作 | 仍有效（不可代改） |
| 19 | 体量 L2 | AGENTS.md 27,411B > 注入预算 16,384B | `handoff.py handoff` **主动拒绝覆盖**（识别为人工索引壳） | 需人工裁口径，不强推（工具边界优先） |
| 20 | 体量 L3 | 交付物 111MB / 整仓 227.8MB | 逐轮录制中间件 `video_raw/` 55.2MB + `demo_video_raw.webm` 25.8MB 全部 gitignored 且未跟踪 | **本轮回收 81.0MB**（哈希一致，成片未动） |
