# 2026-09-26 会话日志 · 卷10（r39 · A-project-better 遗留盘点轮）

> 第 4 次重发同一条对标指令，且显式点了 A-get-memory / A-project-better / A-project-handoff /
> A-memory-start / A-skill-manager。台账第三轮零实质漂移 ⇒ 这轮不再换观测面，改按
> A-project-better 的 Step 0 回答"到底还欠什么、其中哪些其实已经不成立了"。

## 一、Step 0 台账（21 条，逐条实测 + 取证 20:23–20:31）

分布：P0 只能老大做 2 条 · 仍有效 8 条 · **已完成却还挂在待办/差距里 4 条** · 本轮执行掉 3 条 ·
采集结果为"无"4 条（根 TODO/TASKS 无文件、ROADMAP 无未勾选项、task-card 无挂起、
`memory/` 非 junction 且全局无副本 ⇒ 排除 R272 的"两仓各跟踪一份"噪声源）。
全文落 `07-next-steps.part28.md`（台账表）+ `part29.md`（处置与缺陷登记）。

门禁偏差如实记：Step 0 要求按钮确认后才进 Step 1；本轮按用户令「严禁询问、自动化任务」直接续跑，
**台账照常编号留证**，不假装拿到确认。

## 二、台账抓出的两类"假欠项"

1. **05 三条"仍存差距"其实早完成**：`/api/emotion` 前端接线（`chat-agent.js:189` 后端为准＋回落本地）、
   PWA（`src/sw.js` + `src/manifest.webmanifest` 实测存在）、`app.js` 拆分（`src/js/` 已 12 模块）。
   ⇒ 删除线保留原文 + 标注实测状态，明细迁 `05…part12.md`（R241 只加注不改写）。
2. **依赖队列 SLA 已满足**：跑 part26 登记的那条命令 `gh pr list --author app/dependabot`，
   OPEN 只剩已判定不做的 Spring Boot 4 ⇒ 该 P2 打勾留证。

## 三、本轮执行（四条）

- **#7 Docker 实测面**：`java-build` 加两步 —— `docker build -f server/Dockerfile`（context=仓库根）
  + 容器起服三项自证（`status:UP` / `indexFound:true` / `vendorFound:true` + 首页 200）。
  只 build 不算数：COPY 路径坏、瘦 jar、镜像内缺前端都可能"build 成功"。
- **#20 体量**：回收 `demo_video_raw.webm`（25,800,267B）+ `video_raw/` 7 个帧件（55,203,953B），
  实测全部 gitignored 且未跟踪 ⇒ 迁 `_trash/`，**移动前后拼接 sha256 一致、原位清空**，成片 mp4 未动。
  整仓 227,817,951B → 146,815,984B。
- **注入成本**：README「✅ 验证」4,745B 整卷迁 `docs/quality-gates.md`（docs/README.md 建索引不留孤文件）
  ⇒ README 19,531B → **15,834B** 回到 16,384B 预算内；07 壳经 trim-shell 3,845B → 2,633B。
  **AGENTS.md 27,411B 不强推**：`handoff.py handoff` 识别它是人工索引壳并主动拒绝覆盖，该边界优先。
- **自己造的红灯自己修**：迁移把 G2/G4/G14 的声明一起搬走 ⇒ 三处报"声称 None"。
  现值数字回 README 一行（G2/G4 绿），G14 审计面改为跟着声明走（README ∪ 迁移目的地，
  **只扩文件集合、不放宽正则**）；扩面用变异验真：docs 里 16→9 立刻被点名 `差 -7`，还原复绿。
